> RATIFIED by the human 2026-09-01 (chat, question tool). One amendment to the conclusion's "one open choice": the recall sequencing question is SUPERSEDED — the human ruled DROP; #165's revival does not happen (the parked roadmap node carries the ruling; the draft PR is the human's own). Everything else stands as written below.

# What else becomes a plugin? — the eighth lowy-electricity sitting (2026-08-31)

Three altitudes (package/orchestrator, module/grok, upstream/pi), two bars (Löwy's four tests AND Hickey), two rounds each, over the #447 tree at `b631f3b1` (the approved plugin system with prefixed built-in claims), the roadmap files in scope, and the ORCHESTRATOR the human's special ask. Full turns in `debates/lowy-electricity-2026-08-31/`; this is the consolidated conclusion.

## The special ask, answered unanimously: there is no `plugin-orchestrator`

The orchestrator is three things and none of them is a plugin: **an agent** (not olai's code), **vault data** (the boot chain, dag, day boards, Inbox law, gate settings — already in Hickey's best position and never to become code anywhere, least of all upstream), and **olai code built for orchestration** — whose shipped pieces already went to their right homes (fleet → plugin-kolu, CI → plugin-odu, timings → the core task model). The roadmap's unbuilt driver family (p3 actions / p4 gates / p5 judgment / p2b wake) was nominated at package altitude and beaten at both others: no binary to probe, no vendor, *population one* (enabled on exactly one serve on earth — a mode in a place's clothing), and the weekly oscillation is DATA oscillating, which declared properties absorb with zero code motion. The three-way sort all seats signed: **the `/orchestrator` page is a core reading** (as `/agenda` is), **the driver is `olai orchestrate` — a process that is a wire CLIENT** with capability handles minted at the composition root (the probe precedent: the root wires, no plugin imports another, the fence stays physics), **and the practice stays vault data**. A finding of "nothing extractable, and here is why" is the finding.

**The architecture question the sitting produced and closed:** plugin-consumes-plugin. The registry NEVER grows a dependency arm (its stupidity is load-bearing — a filter over a plain keyed object; a dependency arm would invent the half-wired state as a declared feature and re-build Gradle). The framework provides nothing (teaching `mergeDisjointGroups` graph policy would complect mechanics with one consumer's roster). The composition root wires capabilities — threaded, not minted, nothing new exported anywhere.

## The slate

**NEXT (already filed, now deepened — the agent-roster lane, unanimous):** one plugin PER AGENT, never one roster plugin (three independent arguments: cadence, per-agent enablement via `--plugins`, the fence). `@olai/acp` stays a GENERAL package (the protocol is the language, not an integration); each agent's adapter **pins and patches ride its own plugin** (`plugin-claude` inherits the 0.66.0-patch debt). A NEW optional `agent` hook on the server half — the existing `probe()` is an MCP server *handed to* a session and an agent IS the session; overloading it puts Claude in the MCP list (refused). Hook conditions: its shape derives from olai's own roster data and imports nothing framework-side; no dummy `defineSurface({})`. `AGENTS` in `@olai/surface` becomes data (the `PropType` one-arm precedent) — the closed union is what makes a fourth agent a core PR today. `models.ts`'s two Claude bets, per-agent marks (generic fallback stays core), and `NoAgent.tsx`'s install sentences all travel. `acp-system-prompt`: the TEXT is one core module (human-ruled, versioned with the binary); the CHANNEL (`_meta` vs first-turn injection) is each agent plugin's.

**Recall (#165, the human's own PR)** — the sitting passed it at all three altitudes with the contributed-matcher hook, sequencing left as the ratification's one open choice. **SUPERSEDED at ratification: dropped entirely (see header).**

**LEDGERED, each behind a named condition:**
- **Forge/GitHub client** — fires on the first module in `packages/` that calls a forge; WEAKENED by pi's PrInfo evidence: kolu's fleet mirror already carries `reviewDecision` + `mergeStateStatus`, so p4's mergeability gate is likely answerable through the socket olai already holds; the item fires only for questions the mirror cannot answer. Never folded into any orchestrator plugin.
- **Push server** — the pull-only ruling comes FIRST (`reminders-notifications` is `#needs-brainstorm`); if pushing is ruled in, the vendor server half is an olai plugin behind the existing `notify.ts` seam (which already IS the receptacle, over the framework's channel — nothing to extract today).
- **`plugin-saatchi`** — fires the day a PAGE draws evidence state (a lane card's evidence chip, a recording as a dressing); then it is plugin-odu with the nouns changed. Until then the pipeline stays the orchestrator's process, correctly outside the binary.
- **Identity providers** — probe-shaped someday (tailscale-identity is population one today); never kolu's.
- **Sync-backend receptacle** — git's only lawful future reframing; unbuilt until a second backend is genuinely wanted.

**REFUSED, with the tests run:** `plugin-git` (the ledger IS the product; `GIT_OFF` is already the mode; the package already has its socket), themes (data, not a place — read before any module exists), gravatar (one rung of identity's ladder), mobile/PWA, fonts/child/index/log/store (`@olai/index` is core acceleration of core's matcher — the cleanest sentence of the sitting), a saatchi plugin today, `plugin-orchestrator` (above).

## Standing negatives (the seventh sitting's roll forward; one new)

- No `surface://<key>/` grammar minted downstream; serve-discovery stays ledgered untripped; `@kolu/surface` and `@olai/surface` never wrapped.
- **NEW: the receptacle stays home.** `packages/plugins` never goes upstream as a "plugin kit" — its mechanics are already upstream (kolu#2222); everything left is vault-coupled judgement (the withClaims fold, the licence consult, the three-door split over olai's packages). A second consumer's honest ask is more mechanics, never olai's judgements. Both routed hook questions resolved olai-side, proving the refusal.
- **kolu's ledger is CLOSED by this sweep**: rooted bundle (merged), screen tail, one-shot submit, two preview-library imports — zero new items. No phase of the orchestrator roadmap asks a new framework mechanic.

## Falsifiers the record keeps

F1: any candidate later needing a framework mechanic beyond the rooted bundle re-opens the upstream verdict. F2: if a pin bump still diffs a general package after the roster lane, the cut was too shallow. F3: if `/orchestrator` ships naming a plugin member from a general package, the fix is more appliance socket, never plugin-orchestrator. F4: a second wanted tenant for git policy/themes/gates falsifies those negatives by population — say so, don't re-run the tests. F5: if the driver ever needs a browser-half cross-plugin edge (a mount wanting another plugin's dressings), the root cannot wire it — name the edge and re-open. F6: if the roster lane's `agent` hook imports anything from `@kolu/*`, its graph-purity argument re-derives.
