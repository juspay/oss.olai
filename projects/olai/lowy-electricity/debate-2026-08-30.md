# Does #447 cut the sockets right? — the seventh lowy-electricity sitting (2026-08-30)

Three altitudes (package/orchestrator, module/grok, upstream/pi), two bars (Löwy's four tests AND Hickey's decomplection), two rounds each, over PR #447 at `e9212c69`. Ratified by the human 2026-08-30 (~17:50 ET), with two routing rulings recorded at the end.

## Verdict on the PR itself

**The cut is right — all three altitudes.** The receptacle strip (`packages/plugins`), the sibling composition through the framework's own `composeSurfaceContracts`/`exposeFaces`, kinds-as-data with built≠enabled, the probe absorption, the two graph-purity lints: each passed both bars explicitly, mostly with structural proof (the fence makes opacity physics; `--plugins ""` runs the vault test in CI). Nothing found blocks the merge.

**One MUST-class gap, by the PR's own bar** ("licensed by declared KIND, not key name"): the TAB's dressing licence is still name-matching — `Dressing.kind` is the property KEY; the server follows the declared kind, the browser follows the key, so a vault declaring `terminal` on a key named `pty` works on the server and draws nothing in the tab (grok r1, confirmed all round). **The agreed close** (grok r2, pi-confirmed app-side, orchestrator-adopted): not a plugin wire member — a **format answer table beside `doors`** on the page stream, minted by the same consult (`from`, `prop`, `value` → the contributed WORD when the enabled vocabulary claims the value). Declarations still don't travel; the tab still receives answers; `Dressing.kind` becomes the word.

## Refactor-round items (cheap; the author is warm; all three altitudes concur)

1. `OlaiPlugin` drops its ghosts: `kinds`/`runtimeHalf`/`probe` as `unknown` (they live on `./server`), `ownedFile` (relocate to the server half or delete), `testDrivers` (population zero — Löwy's own rule).
2. The render half extends `PluginWire` the way the server half does (one identity, three doors — the type catching up to the graphs).
3. Pin the three-roster name-set equality (`WIRES` = `PLUGINS` = `SERVERS`) as one test — the missing lid on the three-graphs cost.

## Post-merge lanes (each on the human's dispatch word)

1. **The appliance fold** — one identity per appliance: `plugin-kolu` absorbs the wire slice and `kolu-ui` (hops the graphs already pay for). THE RESIDUAL that shapes the lane (grok r2): the DIAL (padi client, detect, koluHalf) today lives behind a package wall that names no olai package — the compiler enforces what a door-walk cannot (a door fence can't tell `koluHalf.ts` from `claimants.ts` inside one package). Either the dial stays its own package (two per appliance, end state) or the fence grows a file-level no-`@olai/format` claim for dial modules (one package). Rule at that lane's design; not this PR.
2. **The agent roster as plugins** (claude/opencode/pi out of `chat/src/agents/`) — unchallenged at all three altitudes; the fastest-oscillating volatility still hard-wired in a general package; `OlaiPlugin`'s own header says an agent is "this shape with most fields empty."

## The upstream ask to kolu (FILED ONLY — the human ratifies before any dispatch; kolu's own /be+/ci process; the drishti pair paid by additivity)

**One capability: the rooted bundle** — a composed wire that carries an unprefixed ROOT beside the sibling bundle. Evidence: olai rebuilt the framework's turnkey client seam by hand (~200 lines, `web/src/client/wire.ts`) because `connectSurfaces` refuses an empty sibling map and derives its probe target as `Object.keys(surfaces)[0]` — while both primitives underneath (`createSurfaceSocket`, `createLiveSignal`) already implement the omitted-key root path. grok's source-read (r2) found NO module-level defense of the derivation beyond "the no-root default of a siblings-only seam"; the framework's own wiring moved four times in one PR window (identity echo, required `retired`, `surfaceWsUrl`, async dial) and every hand-assembling consumer re-derives each move.

The shape (pi r2, grok-confirmed): **threaded, not minted** — a root slot grown on the existing twin doors (`connectSurfaces`; an `exposeFaces` twin establishing disjointness by construction), plus exactly ONE new export (the counted, disjointness-proving group merge — already spelled privately three times across kolu and olai). Absent root = today's behavior; existing consumers recompile untouched.

**The acceptance test travels with the ask**: a three-hunk, grep-able olai diff — `wire.ts` collapses to a seam call + genuine residue (the `retired` policy, the readout's "olai" word); `compose.ts` deletes or thins to re-exports; `faces.ts`'s two fuse sites collapse. Falsifier: if any of `createSurfaceSocket`/`createLiveSignal`/`surfaceClientsHealth`/`createSurfaceReadout`/`fuseGroups`/`fuseFaces` survives in olai app code as MECHANICS after the seam lands, the socket was cut wrong.

## Standing negatives (adopted by all)

- No `surface://<key>/` URI grammar minted downstream before kolu rules the sibling-MCP question (the PR's own filed ask stays the deliverable).
- Serve-discovery as a reserved member stays LEDGERED, not built (olai's `plugins` cell answers product questions a framework member cannot absorb). Filing condition: a second app hand-rolls discovery, or the amber readout becomes operator noise.
- `@kolu/surface` is never wrapped or extracted — the extraction-rejected / receiving-not-immunized line is the record's scope sentence.

## The human's rulings at ratification (2026-08-30)

- **The licence gap fixes IN #447** — the PR meets its own bar before merge; the format answer-table close as specified above.
- **The lane's hold STANDS** — the refactor round (which will carry the licence fix and the three hygiene items) does not run until the human has read the tree themselves.
