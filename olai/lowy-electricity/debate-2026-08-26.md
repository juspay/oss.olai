# How should kolu code live in olai? — the fifth sitting (the kolu-maintenance debate)

| | |
|---|---|
| **The question** | The human: *"I feel like our Kolu integration code in Olai is still complecting/intertwined."* Is it — and what maintenance shape does the evidence demand? |
| **The frame** | BOTH lenses read in full by every seat: Hickey (interleaving / fragmentation / concept multiplication) and Lowy (volatility encapsulation / blast radius), plus the receptacle sittings' four tests for any extraction candidate. |
| **Debaters** | **fable** (Claude Fable, orchestrator-debater — packaging/walls) · **grok** (Grok 4.6 — upstream into kolu). *The module chair collapsed*: pi, then opencode on the human's retry, both died on the kimi-k3 provider outage with zero turns; the human ruled two-seat, chair duties absorbed by the survivors. |
| **Format** | Three rounds (openings, rebuttals, closings); turn files local working material, never committed |
| **Result** | **Converged, unanimous.** The integration is NOT complected where the feeling pointed — it is intertwined in exactly two places, both fixed by deletion-shaped payments, zero new olai packages (fifth sitting in a row). |
| **Date** | 2026-08-26 |

## The sitting in one sentence

The five kolu homes in olai are genuinely independent concerns behind real walls; what the human's gut detected is two leftover voltmeters — a visual vocabulary olai restates (already scheduled to die with dock-row, but with two ratified documents contradicting each other about the wire) and maintenance machinery that hand-tracks kolu's import graph.

## The findings, in plain words

**1. The dock-row charter collision — resolved by DELETION, before the olai-side fold is written.** `@olai/surface`'s kolu half refuses padi's state machine on olai's wire (`kolu.ts:12-27`); the ratified dock-row design requires the row to draw exactly that state machine. The unanimous shape: when the row lands, olai's whole visual restatement dies — `DotFace`, `LIVE_FACES`, `face.ts` and its test, `.olai-dot` and its keyframes, `FACE_SAYS`. The fleet wire stays olai's flat projection carrying what the row's prop bag declares plus `owner` (fleet.ts's own law: a field crosses when something draws it); agent state rides as `Schema.NullOr(Schema.String)`. **No olai-declared closed set of kolu state, ever** — the closed set's one home is the dock-row package's prop bag, which exports its own narrowing guard (unknown states degrade visibly, never silently). `@olai/surface` never imports `@kolu/terminal-vocab` — the literals are not exported as an array, and the import would compile the per-agent schema graph into the outline wire spec. The type fence stays at `rowOf`. Swept for recurrence: `KoluStatus` is the only other olai closed set near kolu and it stays — it describes olai's dial, which kolu has no schema for.

**2. The hydrate machinery — one payment into kolu, two screws.** Where the "intertwined" feeling mostly lives: a hand-written 30-name hydrate list (7 on master — one consumer package quadrupled it), an osfacts pin with its own guard script, a `.d.ts` olai ships for a call no olai source makes, and a hydrate script byte-identical with odu's by comment-discipline. (a) Kolu ships the **leaf entries** its own README names (`padi-client/README.md:109-114`) — olai then deletes the osfacts pin, `check-osfacts-pin.sh`, `types/bun-process-signals.d.ts`, and the residual members. (b) Kolu ships the **hydrate script + a closure emitter** as consumer tools riding the pin (it already computes `declaredDependencyClosure`) — olai's `nix/kolu.nix` keeps only a seed list; odu's script twin dissolves. The npm-registry temptation was pressed and killed on all four tests: every kolu package is `private: true`, the pin is already the receptacle for "which kolu," and raw-TS hydration is how kolu#2216's contract gaps surfaced in olai's `tsc` pre-merge — a tarball trades that away.

**3. `screen.text` tail stays a separate filed ask** (`kolu-screen-tail`) — it does not ride dock-row (two asks in one contract bump is pin-cost). If the polish lane's screenshot pane re-opens the screen.\* procedure, the tail batches with that payment.

**4. The five homes are composed, not complected; chat is a twin.** Surface vocabulary / kolu-client / server join / web UI / chat's MCP detect: four independent concerns plus exactly one fragmented concept (the visual vocabulary — one concept in four files with test-guarded coherence, dying via finding 1). The human's question — *"can chat/src/kolu.ts use kolu-client instead?"* — closes as **no** from both lenses: a one-shot MCP spawn-time probe vs a standing reactive surface mirror, zero derivable shared code, the probe already paid upstream (`@kolu/detect`, kolu#2168). The shared `PADI_SOCKET` string is not extracted either (a constant would add a dependency edge between two integrations whose independence is the design — its proposer retracted it). Remedy: header cross-reference paragraphs so the five homes carry their own map.

**5. The wall, restated.** The physics that must survive: **only `@olai/kolu-client` speaks padi's wire; the browser never dials padi.** The ceremony, retired: "only kolu-client says `@kolu/*`" — already loose in letter (`fleet.tsx` type-imports the fold machinery), deliberately broken in spirit by dock-row's UI packages.

**The blast radius, counted (the acquittal).** An undrawn padi field moves zero olai files; a new agent-state literal moves zero (kolu's `satisfies never` fence + olai's pinned grey fold); a drawn field moves three product files plus three fixtures — Löwy's "contemplative" answer. The expensive rows are the pin bump (seven machinery files — finding 2 kills most of the row) and a socket-protocol change (seven files, correctly concentrated in kolu-client).

## Corrections the debate produced (why it's worth running)

- **fable retracted its own headline repair**: importing `AGENT_STATES` from `@kolu/terminal-vocab` — grok grepped; the array does not exist (the literals live per-agent-package, composed as an inferred union), and the import would put the per-agent graph into the outline spec. A recall-vs-read failure, caught. Also retracted: "the whole `FleetTerminal` reaches the component unused" (five fields idle, not eleven).
- **grok retracted r1's "FleetTerminal restates kolu vocabulary"** — its own blast table's two zero-rows acquit the projection as a genuine translation wall; **upgraded** its paroled hydrate-script finding to a headline after fable's attack; **retracted** its own `PADI_SOCKET`-export nit after fable's dependency-edge argument.
- The seats swapped signatures on the central finding: fable brought the collision, grok brought the surviving repair.

## Filed on ratification (the human, 2026-08-26 midday)

The human's dispatch ruling: **no new PRs — both fixes ride the existing lanes.** The kolu-side work (leaf entries, closure emitter, the row's narrowing guard) rides the in-flight `dock-row-export` PR by its existing author; the olai-side deletion + fold shape rides the existing olai lane (#405's author) when the kolu head exists. The fold constraint is written on the `dock-row` roadmap node so the olai-side work cannot be briefed without it.
