# Cordis for olai: from a plugin registry to a plugin runtime

*2026-09-02. A fresh study of DeepSeek Harness's plugin system and the framework under it (Cordis), against olai's `packages/plugin-api` at `bd40ea03`, and a proposal. Nothing olai ruled before is taken as given here; §7 re-tries each ruling on merit. Sources: the paper "A Programming Paradigm for Spatiotemporal Composability" (arXiv 2608.25512, Shi/Zhang/Cui, DeepSeek-AI and PKU), `deepseek-ai/deepseek-harness` (`docs/architecture.md`, the Cordis primer and tutorial, the generated event catalog, `packages/core/{tools,agent,agent-loop,scope,system-prompt}`, `packages/guard/timeout-policy`, `packages/preset/agent-presets`), and `cordiverse/cordis` v4 (`packages/{core,loader,hmr,include,group}`).*

---

## 1. The one-paragraph version

> **How to read this document (added 2026-09-04).** §§2–5 are the proposal as written before anything was built, kept as the record of why; where a passage was overtaken it says so in place. What olai *is* now is §6 (the phases, with what each landed and every ruling of the human's), §9 (the promotion track) and §10 (the table of which Cordis capabilities are used, adapted, deferred or left out). Two repairs to the bridge the phases stand on are phases 12a and 12b, from the review in [juspay/oss.olai#1](https://github.com/juspay/oss.olai/issues/1). Where the proposal text and §6 or §10 disagree, §6 and §10 are current.

olai's plugin system is a **registry**. A plugin is a value; three `as const` arrays list the values; `--plugins` filters them once at boot; the composition root builds a `PluginServices` blob per plugin and pushes it in. It answers "which plugins are in this build" precisely, and nothing changes after the process starts. Cordis is a plugin **runtime**. A plugin is a function that installs *revertible effects* into a shared *context*, declares the services it needs (`inject`), and the runtime activates it when they exist, tears it down when one leaves, and re-applies it when it returns. Every registration carries its own undo. Everything else is a consequence: runtime enable and disable, hot reload, per-session and per-agent scoping, plugin-consumes-plugin, a declarative config tree with overlays, out-of-tree plugins without a rebuild, and plugins an agent writes and mounts while olai keeps serving.

The proposal: **make Cordis the runtime under olai, server and browser**, keep the judgements that survive re-examination (the vault's row beats the plugin's claim; the doorbell cannot read; failure prose is the plugin's; the browser receives answers, not rules), and overturn the ones that were limitations wearing a rule's clothes (compiled-in only; no plugin depends on a plugin; enable is CLI-only; the orchestrator can't be a plugin). The place it pays off first is **node agents**: many concurrent sessions, lazily spawned, reaped when idle, each with its own MCP servers, wake scope and write fence. That roadmap is a lifecycle problem, and Cordis is a lifecycle runtime with a metatheory behind it.

---

## 2. What Cordis is, in olai's words

The paper names two dimensions of dynamic composability and lifts each from a static type discipline to a runtime mechanism.

| Paper | Cordis API (v4) | olai today |
| --- | --- | --- |
| **Revertible effect** — a context transformation paired with its inverse; the runtime holds the inverses and runs them LIFO on unload (§3.1) | `ctx.effect(() => disposer)`; the body may return one disposer, a promise of one, or a (sync or async) generator that *yields* disposers as setup proceeds. `ctx.on`, `ctx.plugin`, `ctx.provide` and `Service` construction are all effects | none. `PluginServer` has `revision()`, `published()`, `unloaded()` and no teardown; a server half lives for the process |
| **Reactive coeffect** — a plugin declares required keys; every context change is classified activating / deactivating / neutral against the declaration and drives its lifecycle (§3.2) | `export const inject = ['vault', 'deliveries']`. A fiber computes an *epoch* from the uid of each injected service's provider; missing → `PENDING`; a provider replaced (new uid) → unload then reload; a provider gone → unload | `PluginServices` blob; a plugin cannot depend on a plugin; nothing re-resolves |
| **Context** — one object mediating both; a service claims `ctx.<key>`; child contexts derive from parents; property access is a `Proxy` that enforces the declaration at the point of use (§5.1.4) | `class Kolu extends Service { constructor(ctx) { super(ctx, 'kolu') } }` + `declare module 'cordis' { interface Context { kolu: Kolu } }`; `ctx.get('kolu')` for an optional dependency | `PluginServices` (server), `AppFurniture` (browser), both structural blobs re-declared by each plugin |
| **Fiber** — one instantiation of a component, with lifecycle state and a committed view of its dependencies (§4.1, §5.1.3) | `ctx.plugin(fn, config)` → `Fiber`: `PENDING → LOADING → ACTIVE → UNLOADING → DISPOSED`, or `FAILED` with the error on the fiber; `fiber.dispose()`, `restart()`, `update(config)` (runs an `internal/update` waterfall so HMR or a settings page can veto or replace the restart); `ctx.registry` enumerates them | BUILT vs ENABLED, decided once |
| **Isolation** — the same key resolves to a different binding inside a realm (§3.2.3) | `ctx.isolate(key)` derives a child context whose realm table shadows one key; loader `isolate:` on a group | none |
| **Interception** — metadata carried on a context, merged right-biased when a key is *used*, so an enclosing context constrains a component without editing it; changing it triggers no reload (§3.2.3, §6.3) | `ctx.intercept(key, metadata)`; loader `intercept:` | none |
| **Typed events**, five dispatch modes | `emit` (fire and forget), `waterfall` (around-middleware: `(…args, next)`; return without `next()` to short-circuit), `parallel` (`allSettled`), `serial` (in order, first non-nullish wins), `bail`. Listeners are effects, so they unregister with their plugin | ad hoc: `probe()`, `revision()`, `published()`, `unloaded()`, `wake` |
| **Declarative loader** — entries `{id, name, config, disabled, inject, isolate, intercept}`; groups; includes; patches that override a row by `id` or `insert` rows; per-field reconciliation (only `config` changed → `fiber.update`; `disabled` → dispose; `name` → rebuild); `!!js` expressions evaluated against the plugin's own `ctx` (§5.2.1) | three arrays in three files, six edits per new plugin, `--plugins=`, a nix list |
| **Hot module replacement** — changed modules classified by import-graph reachability; stale entries disposed (every disposer runs) and re-mounted from the re-imported module, with rollback if the import throws; no `module.hot.accept` boundaries (§5.2.2) | none |
| **Confluence** (Theorem 80) — whatever sequence of loads, unloads and replacements happened, the quiescent state is observationally equivalent to a from-scratch load of the final configuration. *Under the paper's conditions, which olai claims and does not extend: effects are disciplined and independent, the dependency graph is acyclic, each completed activation installs all keys in that component's declared provision set (Definition 76; a component whose dependency is absent may stay pending), failed fibers are excluded, and the equivalence is over modelled internal state, not over emissions already sent (§§4.3–4.4). Olai's evidence for its own runtime is tests, the flip and restart scenarios, never a proof.* | why `disabled: true`, patches, `fiber.update` and HMR are safe to reason about | "disabled = absent" holds because the filter runs once; a runtime toggle would have no such guarantee |

Three things the paper says that matter to olai specifically:

- **The boundary is per location, not per medium** (§6.1). A location the system can modify exclusively and restore lies inside and is reverted; a file another program reads or a message already sent is an *emission* that crosses the boundary and is compensated or withheld, never reverted. Vault writes and doorbell deliveries are emissions. Cordis reverts registrations, not the world.
- **A key whose value is a table of independently identified entries is commutative; an ordered chain is not** (§3.4.2). Registries of tools, dressings, kinds, listeners are the first kind, so plugins can be unloaded in any order. Middleware chains are the second, so their order is imposed from outside, by `inject` ordering or `prepend`. This is a design rule for every olai service below.
- **Self-evolving agent harnesses are the motivating example** (§1.2.2) and the stated next validation (§8). dsh already ships it: a model-facing tool defines a versioned package from source, a person approves, the host half mounts as an ordinary fiber, retract is dispose.

## 3. How DeepSeek Harness uses it

Every part of dsh is a plugin, including the model adapter, the tool registry, the session log and the agent loop. "There is no privileged core to patch: you extend dsh by mounting a plugin beside the others, and registrations are effects that unwind when their plugin unloads." The load-bearing shapes:

**A service is a key on `ctx` whose registry methods return disposers.** From `packages/core/tools/src/index.ts`:

```ts
export class ToolRuntime extends Service {
  static inject = ['systemPrompt']
  static Config = z.object({ mode: z.union([...]).default('native'), ... })
  constructor(ctx: Context, config: Config = {}) { super(ctx, 'tools'); ... }
  register(definition: ToolDefinition): () => void {
    return this.layers.effect(this.ctx, layer => layer.tools.insert(name, definition), { label: 'tools.register()' })
  }
}
```

`ctx.tools.register(...)` attaches the disposer to the *calling* plugin's fiber; unloading that plugin unregisters its tools and `tools/change` fires so prompt assembly re-derives. The same shape for `ctx.llm` adapters, `ctx.systemPrompt.section(...)`, `ctx.commands`, `ctx.jobs`, `ctx.fs` providers, `ctx.shell` backends, `ctx.subagent` providers, `ctx.slots` in the browser.

**The smallest plugin is a function with `name`, `inject`, `apply`.** `packages/guard/timeout-policy/src/index.ts`, whole:

```ts
export const name = 'timeout-policy'
export const inject = ['tools']
export function apply(ctx: Context): void {
  ctx.on('tools/execute', async (exec, next) => {
    const timeoutMs = ctx.tools.get(exec.name, exec.agent)?.timeoutMs
    if (timeoutMs === undefined) return next()
    using d = deadline(exec.signal, timeoutMs, TOOL_TIMEOUT)
    const result = await next()
    if (timeoutOf(d.signal, TOOL_TIMEOUT) !== undefined) return toolTimeoutResult(timeoutMs)
    return result
  })
}
```

No registration table, no roster edit. Mount it from a config row and it wraps every tool call; drop the row and it is gone.

**A seam is three roles.** Service Definition (an abstract `Service` declaring the interface, e.g. `abstract class SessionPersistence extends Service` with `create/open/flush/stat/list`), Provider (a concrete subclass mounted from config, `session-persistence-jsonl`), Consumer (a tool or the loop). "Pointing the filesystem and subprocess providers at a remote sandbox moves Bash, PTY and LSP with them, with no provider forks."

**Per-agent scope is a tagged child fiber, not `isolate`.** `packages/core/scope/src/index.ts`:

```ts
export function createScope(ctx: Context, key: ScopeKey): Scope {
  const fiber = ctx.plugin(scope)                       // a no-op plugin; its fiber owns the registrations
  const scoped = fiber.ctx.extend({ [kScope]: key })
  return { ctx: scoped, rawDispose: fiber.dispose, dispose: () => (disposing ??= quiesceFiber(fiber)) }
}
```

and `Agent` does `this.scope = createScope(loopCtx, this); this.ctx = this.scope.ctx.extend({ agent: this })`. Everything an agent's plugins register through `agent.ctx` (tools, prompt sections, listeners) is an effect on that scope's fiber; disposing the agent quiesces the fiber and unwinds all of it. Scoped events (`agent/pre-step`, `tools/pre-execute`) deliver only to listeners registered under that agent's scope chain. `isolate:` realms are layered on top by presets, so two sessions mounting the same preset never collide on `ctx.compaction`.

**The loop is events, and the dispatch mode is part of the contract.**

```text
turn/start
  claim next-step input plus one queued message
  assemble prompt sections + tool schemas
  -> agent/pre-step                   waterfall: reject | enter(messages)
     step/start
     agent/request -> llm/stream -> assistant/chunk* -> assistant/message      (waterfalls)
     tool/call* -> tools/pre-execute -> tools/execute -> tools/post-execute -> tool/result*
     step/end
  -> agent/turn-stopping              serial
turn/end
```

Compaction, plan mode, Claude-Code-style hooks, a repeat-tool guard, time context and tmux context are each a plugin listening on one of these. Durable facts (`turn/*`, `user/message`, `assistant/*`, `tool/*`) go to the session log; **model-visible means logged** is asserted at runtime. (olai's replay-misattribution RCA in this vault is a violation of exactly that invariant.)

**Composition is layered config.** A *profile* (`web`, `headless`, `sdk`, `acp`) stacks *bundles*, each a `cordis.patch.yml` of rows plus the code they mount, then the user's patch, then `--patch` overlays. A patch is `- id: typert-loader\n  disabled: true` or an `insert:` block. `dsh --profile web --dump-config` prints the tree the machine boots; `dsh plugin` installs an out-of-tree package into a profile. From `presets/standard/agent.cordis.yml`:

```yaml
- id: tool-bash
  name: '@deepseek-ai/dsh-tool-bash'
  disabled: !!js process.platform === 'win32'
- id: planning
  name: cordis:group
  group: true
  isolate: { planMode: true }
  config:
    - id: plan-mode
      name: '@deepseek-ai/dsh-plan-mode'
```

## 4. What olai gets

### 4.1 Teardown, therefore runtime enable, failure containment, reload

Today "disabled" means the entry was never added before composition. That works once and cannot express: turn odu off for this serve without a restart; a plugin whose `serve()` throws (padi socket gone at boot) staying failed while the rest run; a plugin re-reading its vault config file without a restart; a developer editing `olai-plugin-kolu` and seeing it re-mount.

With every registration an effect, all four are `fiber.dispose()` and `ctx.plugin(...)` again, and the loader performs them from a config edit. A plugin that throws during `apply` lands in `FAILED` having installed nothing (Corollary 69), siblings untouched. The property olai's own doc calls the design's best, "disabled is a state the framework's composition already expresses", now holds for *registrations* once a teardown or reconciliation has settled, instead of only at boot (while an asynchronous unload is still running it is not yet true, and nothing here says otherwise): they are scope-owned, and after a successful cleanup their releases remove the departing plugin's contributions. It does not hold for an emission already sent, and "the fiber is gone" is not by itself proof that a release completed (Cordis logs a throwing disposer and moves on); the rollback and teardown tests are what establish that each registry meets the contract (juspay/oss.olai#1, finding 3).

### 4.2 Services, not a blob; plugins that need each other

`PluginServices` is seven fields and growing, built per plugin by hand in `runtime.ts` (`doorFor(plugin.name)`), and every plugin receives all of it. Under Cordis each field is a service a plugin names:

```ts
// olai-plugin-odu/src/server.ts
export const name = 'odu'
export const inject = ['vault', 'deliveries', 'kinds', 'surfaces', 'wakes']

export function apply(ctx: Context) {
  ctx.kinds.register({ kind: 'worktree', takes, admits: isPathShaped })   // the service composes odu-worktree from ctx.fiber.name
  ctx.surfaces.register(surface, oduHalf(...), faces)                       // sibling under the fiber's name
  ctx.vault.revision(snapshot => doorbell.consider(snapshot))            // replaces PluginServer.revision; a contained door, not a bare emit
  ctx.wakes.register({ subject, from, waiting, kinds, faults })
}
```

The stamp that today is threaded by the root ("core marks that row with the plugin's name, stamped from the registry binding and never from the caller") becomes the fiber's name read by the `deliveries` service at registration, the same guarantee with no threading. A plugin that does not name `deliveries` never sees it. And a plugin may name **another plugin's service**: the in-flight `plugin-spaces-mirror` lane wants kolu's fleet and odu's runs, an edge the current design can only wire by hand at the root. With `inject = ['kolu', 'odu']` it waits for both, unloads when either leaves, and touches nothing in core. *As built (phase 7), this is narrower than written: a plugin may stand behind a service only from `OFFERABLE`, five core-owned keys, a deliberate limitation so that no plugin can shadow a core service. The open form is phase 12b, plugin-owned keys stamped `<plugin>.<word>` from the fiber's name, core's keys staying closed.* Cycles are not a runtime hazard: two plugins each declaring the other's key never activate, and the paper says a runtime can report it at load from the declarations alone (§6.5); whether the pinned runtime does is unverified and not claimed (§10).

### 4.3 Chat as events; the doorbell as a waterfall

`probe()` must answer both halves at once, an invariant with an incident behind it (asking twice started a daemon twice). A waterfall gives that invariant for free, one dispatch per session start:

```ts
// core
const start = await ctx.waterfall('chat/session-start', { servers: [], missing: [] }, s => s)
// olai-plugin-kolu
ctx.on('chat/session-start', async (start, next) => {
  const probed = await probe(ctx.env)
  probed.server ? start.servers.push(probed.server) : start.missing.push(probed.missing)
  return next()
})
```

Delivery policy (idle takes it as a turn; busy holds; nobody there queues) is a `chat/deliver` waterfall whose short-circuiting listener is core's policy plugin, `coalesce` a listener before it. Each rule becomes a plugin that can be read, replaced or tested alone, which is how dsh ships compaction and plan mode.

### 4.4 Node agents are scopes

Node agents are on master (phase 1 of that plan: the roster over `prop:agent`, the door on agent rows, the panel switching, the subtree-memory teaching). What is not on master is concurrency: `agent.ts` still holds one `session: string | null`, so an agent that is not the open one has no process. The plan's phase 2 says "hold a map", and its later phases say sessions spawn lazily, reap when idle, are capped, and each carries its own MCP servers, its subtree as wake scope, and a write rule ("an agent writes only strictly inside its own subtree and asks its ancestor for anything above").

dsh's `createScope` is the exact shape:

```ts
// core: node agents
const scope = createScope(ctx, node)                    // a child fiber; dispose = every registration gone
const agentCtx = scope.ctx.extend({ node })
agentCtx.intercept('vault', { writable: subtreeOf(node) }) // the write fence, carried on the context
agentCtx.plugin(session, { engine: node.props.agent })    // probes run here; MCP servers register here
```

Reaping an idle session is `scope.dispose()`; a wake for a session with no scope creates one (the paper's "a deactivation may chain straight back into an activation"). *As built in phase 6 this sketch is an adaptation, not Cordis's context model: a node agent is an Effect scope, a session a fiber under it, and the fence a per-session ticket minted by the composition root and checked in the write gate at the moment of the write. That is policy consulted at invocation, which is what mattered, and it is per session without a reload; it is not `ctx.intercept`, and no `isolate` realm exists. §10 lists what is omitted.* The proposal read the subtree write rule as **interception metadata**: consulted by the vault service when a write is attempted, installed by the enclosing context, invisible to the session's code, and adjustable at runtime without a reload (§6.3 uses precisely this example: read-only access for a community component, full for a core one). Two node agents on different engines are two providers under two `isolate` realms of the `engine` key. The cap and the lazy spawn are one policy plugin over `ctx.registry`. Concurrency stops being a rewrite of `agent.ts` and becomes the default shape, which is what the human said phase 2 requires ("the ceiling is ours, not the protocol's").

### 4.5 A config tree, not three rosters *(historical: the CLI verbs sketched here, `olai plugin add` and `--dump-config`, were dropped by ruling in phase 8; the panel is the switch and the table. What landed is phase 8's loader surface and phase 10's row config.)*

Adding a plugin is six edits in `plugin-api` today, three of them arrays that must agree in order. Under the loader the build's set is a bundle:

```yaml
# packages/bundle/base/olai.yml
- id: vault
  name: '@olai/store'
- id: kinds
  name: '@olai/format/kinds'
- id: chat
  name: '@olai/chat'
- id: kolu
  name: 'olai-plugin-kolu'
- id: odu
  name: 'olai-plugin-odu'
```

`olai web` and `olai surface` (headless MCP) are profiles stacking this bundle with their own (an `olai orchestrate` profile was dropped 2026-09-04; the mechanism would carry it if it returns). `--plugins=odu` and the nix option write a patch (`- id: kolu\n  disabled: true`). `olai --dump-config` shows an operator the tree this serve boots. A third party runs `olai plugin add olai-plugin-saatchi` and the row lands in their profile: **no rebuild**.

### 4.6 Slots, not four browser registries

`dressings.ts`, `Chrome.tsx`, `Mounted.tsx`, `marks.ts` are four hand-written walks over `ROSTER`. The browser runs its own Cordis app (Koishi's console and dsh's web client both do) and `ctx.slots` is one typed registry: an owner declares a slot (`outline.row.block`, `app.header`, `chat.speaker.mark`) with a cardinality (`single`, `list`, `keyed`, `chain`) and a scope; a plugin registers into it and never imports another feature's component; disposing the plugin removes its cells; disposing the owner collapses its children. The licence consult stays exactly where it is (the server answers the *word*; the browser looks it up) and the lookup is the `keyed` slot `outline.row.dressing`.

### 4.7 Plugins an agent writes

dsh's extensions subsystem: `cordis_define` stores a versioned package from source; `cordis_run` asks the person; the host half mounts as a fiber owned by the session; `cordis_inspect` lets the agent query the live registry and slot tree before writing code; retract is dispose. For olai: a node agent whose subtree holds a small dressing or doorbell and whose olai mounts it, live, under the same teardown as kolu. It is the paper's stated future and the reason not to build a second runtime later.

## 5. What it costs, honestly

**Two composition systems on the server.** olai's server is Effect (`effect@4`); Cordis is a lifecycle runtime over plain host code. They are not rivals for one job. Effect's `Layer` is static composition, built once, with no re-resolution when a provider leaves; Effect's `Scope` is the revertible half without the reactive half. The paper says Cordis is "an overlay, realizable atop a language of either style" (§7.2). The split that works: Cordis for *what is mounted and when*, Effect for *how a mounted thing does its work*. The alternative, re-implementing reactive coeffects and the loader over Effect's `Scope`, is the paper's Section 5 written again without its proofs.

**How Cordis arrives: an npins pin, never a copy.** dsh vendors Cordis into `vendor/` under its own `@deepseek-ai` scope with a sync script. olai does not copy libraries into the repo; this machine's rule, and the repo's standing law, is that the app requires no dependency outside Nix itself. So Cordis comes in the way `@kolu/*` and `@odu/run-client` do: a pin in `npins/sources.json` on `cordiverse/cordis` (upstream, not dsh's rescoped fork, so `import { Context } from 'cordis'` and `declare module 'cordis'` read as written), a `nix/cordis.nix` beside `nix/kolu.nix` and `nix/odu.nix` naming the packages olai's source seeds (`packages/core` as `cordis`, plus `loader`, `include`, `group`, `hmr` as `@cordisjs/plugin-*`) as the `(src, dest)` pairs kolu's `hydrate-kolu-packages.sh` copies into `node_modules` as raw TypeScript, and the pin's closure (`cosmokit`, `schemastery`) riding the same pin or its own. `scripts/check-hydrated-deps.sh` asks its one question of the new pin exactly as it does of the other two: every external the hydrated sources declare is in olai's root manifest at the pin's version, so there is one `cordis` and one `effect`, never two. `just update-pins` walks it forward with everything else, and `npins/sources.json` records the revision, so what the tree compiled against is always in the diff. The API is marked "not yet stable" upstream, which is an argument for the pin, not against it: a moved revision shows as a diff and a typecheck, never as a silent drift inside a copied directory.

**The wire is composed once today, and the spike confirmed it.** `implementSurfaces` and `mergeDisjointGroups` are one-shot over the map they are handed; there is no incremental add or drop on a live group. A `ctx.surfaces` that re-calls them on register/dispose works (sibling tags appear under `surface/<name>/`, dispose restores core's tags exactly), but it re-implements every surviving sibling each time, state survives only because it lives in each plugin's `deps`, and a server holding the previous fused handlers keeps serving them. On the client, `connectSurfaces` takes `surfaces` at the call and `SurfacesConnection` has `dispose` and no `update`, so a roster change is a reconnect. This is a framework ask to `@kolu/surface` and `@kolu/surface-app`, filed before phase 2 starts: live add/drop on a rooted bundle plus `update(surfaces)` on a live connection, or reconnect-per-roster-change documented as the contract. dsh solves the analogous problem with `tools/change` and per-step re-derivation.

**Typing moves from `satisfies` at the registry to declaration merging.** `ctx.kolu` is typed by `declare module 'cordis' { interface Context { kolu: Kolu } }` in kolu's own package; a consumer gets it by `import type {} from 'olai-plugin-kolu/server'`. As strong as `as const satisfies`, more local.

**Async disposers run concurrently** (reverse registration order, but async ones in parallel). Ordered teardown lives inside one effect, ideally as a generator yielding disposers in sequence. Import dsh's defensive-patterns rules with the code: dispose must reach quiescence; contain listener exceptions in the dispatcher; report orthogonal outcomes independently.

**Reversion is bounded by the boundary.** Vault writes, delivered sentences, dialed sockets are emissions; the runtime reverts registrations. Not worse than today, where nothing is reverted, but not undo for vault edits.

**Dependency identity is nominal.** A key is a string; drift between independently built providers and consumers is caught by peer-dependency ranges, not the runtime (§6.6). Name services by package (`kolu`, `odu`, or `kolu.fleet`) the way kinds are prefixed today.

**HMR on Bun: no.** dsh runs HMR on Node under `tsx`, deleting Node's internal ESM `loadCache` entries and `require.cache`, then re-importing the same specifier. Under Bun 1.4 `node:internal/modules/esm/loader` does not exist, a rewritten file at the same URL is served stale, a query string on that URL is also stale, and the old `Loader.registry` global is gone. Only a different file path yields a new evaluation. HMR in dev is off the plan until a Bun-specific cache bust exists.

**Two pin frictions, both throwaway in the spike and owed by phase 2.** Cordis's `package.json` points at `lib/`, so `nix/cordis.nix` rewrites `exports["."]` to `src/` and stamps every file `@ts-nocheck` because olai's `tsc` is stricter than Cordis's. Phase 2 either typechecks the pin under olai's `tsc` (the raw-TS argument the kolu and odu pins already make) or files the strictness delta upstream, and the rewrite must cover every subpath the code imports. Separately, Cordis exports `FiberState` as a `const enum`, which `isolatedModules` cannot consume as a value; the spike pins the numbers locally, and the upstream ask is a regular enum or a const object.

## 6. The plan

Each phase is one PR, green alone.

1. **Spike: done** ([juspay/olai#472](https://github.com/juspay/olai/pull/472), draft, unmerged, nothing in `packages/cordis-spike` ships). The `cordis` pin is in `npins/sources.json`, `nix/cordis.nix` seeds `cordis` core and `@cordisjs/plugin-loader` through kolu's copier, and `just cordis-deps` asks the same question of it as of the other two pins. On real tenants: `disabled: true` at runtime leaves core's tags byte-identical; a throwing `apply` lands `FAILED` with kolu `ACTIVE`; `--plugins` is a `disabled` patch the loader applies at mount and later; the Proxy `ctx` reads fine inside `Effect.gen`; `ctx.intercept` is read through `Service.resolveConfig` on the derived context with no reload. Its answers are folded into §5 and §8, and its five review findings are the constraints on phase 2 below.

2. **The app on Cordis, whole: done** ([juspay/olai#474](https://github.com/juspay/olai/pull/474), merged 2026-09-03 as `35f7fb9d`). One lane, in this order, each step green alone, and the lane merges as one unit or not at all. *Ruled by the human, 2026-09-02: olai is one app, not a client and a server; a PR that finishes the server half and covers the seam with a test does not merge, and a hand-written list held equal to a data file by a test is duplication, not a fix.* The server steps below landed on the branch first; the browser steps were pulled forward from what was phase 5 and belong to the same unit.
   - **File the framework ask first** (§5): live sibling add/drop on a rooted bundle and `update(surfaces)` on a live `connectSurfaces` connection, or reconnect-per-roster-change as the documented contract. If the ask is slow, phase 2 starts with reconnect-on-change and says so.
   - **Re-compose moves into the composition root** (`packages/server/src/runtime.ts`) before anything else moves. The spike's `Surfaces.recompose` re-implements every surviving sibling; the root's version must compose existing runtimes, set the real `plugins` cell in `@olai/surface` (the spike only shape-proved its roster against `PluginRoster`), and swap the served handlers so a live connection does not keep the old fused group.
   - **`PluginServices` dissolves into services** (`vault`, `env`, `clock`, `log`, `deliveries`, `kinds`, `surfaces`, `wakes`), each a `Service` whose registration methods return disposers attached to the calling fiber; the per-plugin stamp comes from `ctx.fiber.name`.
   - **Hooks become doors and one waterfall.** `revision` and `unloaded` became contained subscriptions on `ctx.vault` (`vault.revision(handler)`, `vault.unloaded(handler)`), not bare Cordis events: Cordis's `emit` is a loop with no `try` in it, so a throwing listener would have silenced every later plugin and failed the directory fiber; the door wraps each handler once and warns with the calling fiber's name (the shape `ctx.watching.subscribe` already had). `probe` becomes the `chat/session-start` waterfall (§4.3). `surfaces/published` was declared and never emitted, so it went; the roster cell carries what it would have said. **`unloaded()` is not teardown**: it means the store failed to publish. A half that has teardown beyond its effects returns a disposer from `apply` (xyne-spaces is the first caller).
   - **The three rosters become the base bundle**; `--plugins` and the nix option write a `disabled` patch through include's `applyEntryPatches` (the spike's `pluginsPatch` was the same data as a function); `plugin-api` shrinks to service definitions; `fence.test.ts` keeps every claim except plugin-imports-plugin, and the `cordis` arm in its `FRAMEWORK` regex becomes permanent.
   - **Pin hygiene rides along**: `@cordisjs/plugin-include` and `-group` join the pin; the `@ts-nocheck` stamp goes (typecheck the pin under olai's `tsc` or file the delta upstream); the `exports` rewrite covers every imported subpath; `FiberState` is filed upstream as a regular enum or const object and the local numbering is deleted.
   - **One roster, end to end.** `olai.yml` is the only place a plugin is named. The tab's plugin table (today's hand-written `WIRES` and `PLUGINS`) is generated from that file at build time by `packages/web/src/build.ts` or a step beside it. A third plugin is one row. `rosters.test.ts` is deleted, because there is nothing left to hold equal.
   - **The tab is a Cordis app.** A client `Context` in `packages/web/src/client` with a `slots` service (declared slots with cardinality: `outline.row.block`, `outline.row.chip`, `outline.row.pane`, `app.header`, `app.drawer`, `app.mount`, `chat.speaker.mark`). The four walks over the manifest list (`live/dressings.ts`, `Chrome.tsx`, `Mounted.tsx`, `plugins/marks.ts`) become slot reads; each plugin's browser half becomes `apply(ctx)` registering into slots through `ctx.effect`; `AppFurniture` becomes services the browser half names in `inject`, the move `PluginServices` made on the server.
   - **The tab follows the roster.** Dial core first, read the `plugins` cell, bring in the siblings it names through `SurfacesConnection.redial(surfaces)` (kolu#2223's client half). A plugin turned off on the server leaves the tab without a reload. "Mount assumes OFF until the roster lands" becomes the boot order rather than a guess. The `plugins` cell gets an `equals` so a no-op re-compose does not redial.
   - **Disabled is absent in the tab too.** The bundle ships every built plugin's code, but per-plugin code splitting (a dynamic `import()` per row, generated with the table) means the tab loads and evaluates only the plugins the roster names.
   - **Not started in this lane**: HMR (no Bun cache bust exists, §5); intercept in the store (it belongs on the `vault` service, §8); node-agent scopes (phase 6); the kind vocabulary's boot-time snapshot in `propKinds.ts` (phase 8's debt). Retire `.worktrees/cordis-spike` when the draft closes.
3. **One directory per plugin, named by the plugin: done** ([juspay/olai#480](https://github.com/juspay/olai/pull/480), merged 2026-09-03 as `bc2f322f`, a rename diff with no deferrals). As landed: `packages/plugins/kolu/`, `odu/`, `xyne-spaces/`; the two client packages deleted and folded in behind each tenant's `./appliance` export (kolu's dial sits at `src/client/` because `src/appliance/` already held its UI fold; odu's at `src/appliance/`); `@olai/tests` reaches the fake padi through `olai-plugin-kolu/appliance/testlib`, recorded as two files and one declaration rather than a general-package exception. The brief it landed against: The tenants move to `packages/plugins/kolu/`, `packages/plugins/odu/`, `packages/plugins/xyne-spaces/` (the human, 2026-09-02): the container already says "plugin", and everywhere else the plugin is one word (the row's `id`, `--plugins=kolu`, `docs/plugins/kolu.md`, the sibling key on the wire), so the directory is that word. The package *name* stays `olai-plugin-<name>`: it is the module specifier the row mounts and the shape an out-of-tree plugin would publish under, and bun's workspace finds a package by its manifest, not its folder. `packages/kolu-client` and `packages/odu-client` fold into those directories as `src/appliance/` (or a `./appliance` subpath if a second door is needed) and the two packages are deleted. They exist because, before phase 2, a plugin could not import the interface and the only way to get an appliance out of core was a package that depended on nothing; that reason is gone. They are also the one place the fence's claim is bent by naming rather than by import. xyne-spaces, which arrived after the runtime, is one directory and is the shape. `fence.test.ts`, `prove-fence.sh`'s container derivation, and `check-hydrated-deps.sh` follow the move.
   - **Done means**: the three tenants at `packages/plugins/<name>/`, `kolu-client` and `odu-client` deleted, and `fence.test.ts`, `prove-fence.sh` and `check-hydrated-deps.sh` green on the new paths. Nothing in the runtime or the plugin API changes.
4. **The plugin API is Effect: done** ([juspay/olai#483](https://github.com/juspay/olai/pull/483), merged 2026-09-03 as `31f85927`; the human's own third review round folded before the merge). One PR, a content diff on every plugin's `server.ts`, reviewed on the final paths phase 3 gave them, and before the agents lane writes three more plugins against whatever API exists then. *The human, 2026-09-02: olai uses Effect; for an architecture with no hacks or escape hatches, plugins are written in Effect and Cordis is an engine nobody outside one package sees.*
   - **Why.** After phase 2 the two runtimes meet through bridges in both directions: the composition root wraps Cordis calls in `Effect.promise`, Cordis services call back into Effect through `ring(Effect.log…)`, and plugin bodies are plain TypeScript that reach into Effect code by hand. Each is an escape hatch and every new plugin copies them. Effect's `Scope` (finalizers, LIFO) already is the paper's accumulator; its requirement channel already is the paper's coeffect specification. Two vocabularies for one idea is the duplication this lane removes.
   - **A plugin's `apply` is an Effect**: `(ctx) => Effect<void, never, Scope | Vault | Kinds | Surfaces | …>`. Its revertible effects are `Effect.acquireRelease` and `Effect.addFinalizer` on that `Scope`; a facade in `@olai/plugin-api` runs the Effect inside the Cordis fiber and closes the scope from the fiber's disposer. Authors never call `ctx.effect`.
   - **`inject` is the requirement channel.** The services a plugin needs are the `R` of its `apply`. Because the compiler cannot hand Cordis a runtime list from a type, the plugin declares the list once beside `apply`, typed so the two cannot disagree (a helper that takes the Tags and yields both the list and the `R`). A mismatch is a `tsc` error naming the plugin, the same shape `as const satisfies` gave the old registry.
   - **Services are Effect services.** `ctx.vault`, `ctx.kinds`, `ctx.surfaces`, `ctx.deliveries`, `ctx.wakes`, `ctx.watching`, `ctx.held`, `ctx.env`, `ctx.clock`, `ctx.log` become `Context.Tag`s whose implementations are provided by the facade from the Cordis-side services; `ctx.log` is Effect's logger; `deliver` and `register` return Effects; a registration's undo is a finalizer on the calling plugin's scope. The vault doors become `Stream`s (`vault.revisions`, `vault.unloaded`) and the waterfall (`chat/session-start`) becomes Effect middleware. The stamp still comes from the fiber: the facade reads `ctx.fiber.name` once and provides it as the plugin's identity.
   - **Cordis is confined to one package, `packages/effect-cordis`.** The translation lives there and nothing else does: `definePlugin(effect)` (open a `Scope` on activate, close it on dispose, a throwing Effect lands `FAILED` having installed nothing), the requirement-to-`inject` helper, `serviceTag(name)` (a Cordis key as an Effect `Tag`, provided from the fiber's context), `eventStream` and `waterfall` (events as `Stream`s and Effect middleware), and the loader entry as an Effect. It has no olai noun in it and is tested with toy services: a plugin sits `PENDING` until its `Tag` is provided, finalizers run in reverse on dispose, a replaced provider re-runs the plugin, a `Stream` ends when the plugin unloads. `@olai/plugin-api` holds olai's `Tag`s and event names and imports `effect-cordis`, never `cordis`; no other package imports either, and the fence's `cordis` arm narrows to `effect-cordis` alone. This is also where Cordis's instability has its one home: a pin bump that moves the API changes this package and nothing else, and the next-gen track (§9) mounts the core's own parts through it without going through the plugin interface. It stays translation: the moment it grows an opinion about what a plugin is, that opinion belongs in `plugin-api`. The loader still mounts a module by name from a row; what the row points at is the plugin's `server.ts` export wrapped by `definePlugin`. Cordis stays the engine for the reactive half and the loader, the part worth not writing again; the Effect-native alternative stays named as the other consistent answer and is not taken.
   - **Done means**: three plugins written as Effects with no `cordis` import; the composition root's `Effect.promise` wrappers and the services' `ring(...)` callbacks gone; the bridge is `packages/effect-cordis`, with its own tests; the fence's `cordis` arm narrowed to that package and prove-fence catching a plugin that imports `cordis` directly.
5. **Agents as plugins** ([juspay/olai#486](https://github.com/juspay/olai/pull/486), merged 2026-09-03; one PR, dispatched 2026-09-03 to claude-opus on `agents-as-plugins`). One plugin per engine, never a roster plugin: the three rows share no release clock (Claude's adapter pin moved five times in a month, opencode's zero), `--plugins` enables them one at a time, and the fence gets one more general package that spells no engine.
   - **What exists (on `plugin-api-effect`).** `packages/chat/src/agents/{claude,opencode,pi}.ts` are the three *legs* (each engine's wire bets: `_meta` keys, tool spelling, steering, queue advertisement) over the `Leg` interface in `leg.ts`; `roster.ts` holds a hardcoded `KINDS` (`id`, `leg`, `at(where) => Adapter | null`) and `rosterOf(where)` over it; `models.ts` reads the model out of ACP's own `configOptions` (a protocol reading shared by both agents that name a model, with two spellings its header admits are one adapter's: `id === "model"` and the alias tiers), consumed by `agent.ts` for the header and the restore; `@olai/surface/chat.ts` exports `AGENTS = { claude, opencode, pi } as const` and `AgentId = keyof typeof AGENTS`, a closed union that makes a fourth engine a core PR; `acp/patches/` and `nix/acp-agent.nix` hold the adapter pin and its patches for all three; the no-agent face's install sentences live in `@olai/web`. The engine's MCP servers reach a session through the `SessionStart` waterfall (`plugin-api/src/services.ts`), which still carries two things phase 4 left by name: a plugin pushes `{ name, ask }` and signs its own `name`, and `ask` answers a `Promise<Probed>` where everything else a plugin hands over is an Effect.
   - **The shape.** `packages/plugins/claude/`, `odu`-style: one row each in `olai.yml` (enabled by default; `xyne-spaces` is the opt-in precedent). A new tag `Agents` in `plugin-api/src/services.ts`, provided from a `registry` keyed by the fiber's name: `agents.register({ leg, at, missing, prompt })` where `id` is the fiber's word (the plugin cannot spell another's), `leg` is the engine's `Leg`, `at` finds it on this host (`Adapter | null`; `null` is "not installed", not a fault), `missing` is the install sentence (`NotHere | null`, the plugin's whole sentence, `null` for a shipped-in engine), and `prompt` is the CHANNEL the standing prompt rides (`_meta` vs first turn). The system-prompt TEXT stays one core module versioned with the binary. Each engine's adapter pin and patches travel into its plugin directory (the 0.66.0 patch debt goes with Claude); `nix/acp-agent.nix` splits per plugin or is parameterised by the row. `models.ts` stays in `@olai/chat`, by its own argument (a per-leg copy would be one rule written twice); whether the alias tiers belong on Claude's leg is the lane's call, and it is a server-side call. `@olai/acp` stays a general package: the protocol is the language, not an integration.
   - **Both halves, like every tenant.** Each engine plugin is `server.ts` and `browser.tsx`, mounted the same way kolu's are. The server half registers on `Agents` and answers the session-start door. The browser half registers into slots: its mark on `chat.speaker.mark` (the generic fallback mark stays core, for an engine with no plugin), its row in the engine picker (a new keyed slot the chat panel declares, so the picker draws what the roster names and core spells no engine), its install sentence on the no-agent face (a slot beside the picker's, keyed by the fiber; the sentence is the plugin's `missing.why` spent a second time). There is no model picker in the tab (the header shows the model name the server read), so nothing of `models.ts` is a browser matter. The tab follows the roster, so `--plugins=opencode,pi` draws no Claude mark and no Claude row, with nothing in core knowing why.
   - **Core, before → after.** `rosterOf(where)` reads the `Agents` registry instead of `KINDS`; `AGENTS` becomes data the server sends (an `engines` list beside the existing roster, or a cell), `AgentId` becomes `string`, and every table keyed by it follows; the picker draws what arrived; the no-agent face's rows are each enabled plugin's `missing.why`. A node's `agent` property names an engine id and resolves against the registry; an id no enabled plugin registered is the same absence a missing binary is. `OLAI_ACP_AGENT=""` keeps its meaning; `--plugins=opencode,pi` serves a panel with no Claude row, no probe, no mark.
   - **The session-start door, finished.** The collector keys `asking` by the fiber that pushed, so a plugin pushes `{ ask }` and the name is stamped like every other door; `ask` answers an Effect, and `@olai/chat` runs the thunks as Effects under its bounded concurrency (`AT_ONCE`) rather than as promises. Both are edits on the session-open path this lane already touches.
   - **Done means**: `KINDS` deleted; `AGENTS` no longer a union and no general package spells an engine (fence claim, with a prove-fence mutation); three rows; `acp/patches` split per plugin; `SessionStart` carries no `name` and no promise; every scenario in `packages/tests/features/` that opens a session passes unchanged; the plugins panel draws the three engine rows with the same five states as any plugin.
   - **Not started**: node-agent concurrency (phase 6); dissolving `@olai/acp`; a fourth engine (the point is that it would be one directory and one row, and nothing here should need it to prove that).
6. **Node agents as scopes, in place** ([juspay/olai#494](https://github.com/juspay/olai/pull/494), merged 2026-09-04 with its three SHOULDs landed: boot routes a remembered node session before spawning, `ringing` is on the door, and the doorbells spell no precedence; one PR, after phase 5). *Ruled by the human, 2026-09-03: phase 6 splits in two, the rewrite before the relocation, because #490 measured the lane (`@olai/chat` 20,406 lines, the tab's chat and agents 21,400, 24 general packages that spell `chat`, a one-to-many rewrite of `agent.ts` and `chat.ts`) and a rewrite inside a move is the diff #474's rename-versus-rewrite lesson says not to review.* This lane is the rewrite, inside `@olai/chat` as it stands. It delivers node-agents phase 2 (concurrency) and phase 4 (derived wakes); phase 1 (the roster over `prop:agent-session`, the door on agent rows, the panel switching, the subtree-memory teaching) is on master.
   - **What exists.** `packages/chat/src/agent.ts` holds `let session: string | null`: one live session, so an agent that is not the open one has no process and a wake for it is held, not acted on. The roster's live states are true for one row at a time. Wake scope is a manual pick per conversation in `@olai/state`'s `wake` record, served through `chat.scope` (`scopes.ts`); a plugin's doorbell asks `deliveries.scopes()` and gets `(agent, session, file)` triples. The pinned ACP adapter holds many sessions keyed by id and tears one down at a time, so the ceiling is olai's; `pi-acp` calls `closeAllExcept` inside `session/new`, which forces process-per-session for pi and which `session/resume` (SDK 1.3) makes cheap to reap (#490's finding). #490's write-fence core half (the at-or-under containment answer in `@olai/format` shared with `under:` search; the per-session credential channel; the enforcement point between `plan` and `commit` in `@olai/ops`, with nothing minting a ticket yet) is this lane's: #490 is closed unmerged and this lane cherry-picks that half from its branch and lands it with its first caller, the ticket a session mints.
   - **The shape.** A node agent is an Effect `Scope`; a session is a fiber under it, acquired by `Effect.acquireRelease`: spawned lazily on the first message or the first wake in scope, reaped on an idle deadline, capped, with the ACP process, the MCP servers from `SessionStart`, the inbox and the delivery door all acquired on that scope, so reaping is one `Scope.close` and nothing is remembered. No plugin is needed for any of this; the scopes hang off `Chat.make`'s own scope today and off the chat plugin's `apply` after phase 7, unchanged. `let session` goes. The panel routes turns and deliveries by node, not by "the" conversation; the roster's states are computed per row from the live map.
   - **The write fence.** "An agent writes only strictly inside its own subtree and asks its ancestor for anything above" is a per-session provision of the vault's write door narrowed to the subtree: a ticket minted when the session spawns, carried on the credential channel #490 built, and enforced in `@olai/ops`'s write gate against it. A write outside is refused naming the ancestor. Not in the prompt.
   - **Derived wakes.** For a session bound to a node, `deliveries.scopes()` derives the file from the node's subtree, so kolu's and odu's doorbells change nothing; the manual picker survives only for unassigned conversations; an agentless wake climbs to the nearest ancestor node agent, and the orchestrator ends up the root catcher. `@olai/state`'s `wake` record keeps only the picks for unassigned conversations.
   - **Done means**: `let session` gone; two node agents converse at once, both roster rows live, in a scenario in `node_agents.feature`; a wake for an agent that is not on screen is acted on; an idle session is reaped and its next message respawns it with the subtree as memory; the picker is absent on an agent-bound conversation; a write outside the subtree is refused with the ancestor named; a session's whole footprint is one scope, so the relocation in phase 7 moves it as it is.
   - **Not started**: the Unassigned migration UI and the distillation turn (node-agents phase 3); agency (an agent creating child nodes and seating sessions); the Spaces binding; which engines can hold many sessions per directory cleanly (recorded per engine as the lane meets it).
7. **Chat is a plugin** ([juspay/olai#498](https://github.com/juspay/olai/pull/498), merged 2026-09-04; the one SHOULD stands as an open product call: the `legacy-key` finding lands on `_olai/Properties.olai` and by the error model darkens that file until the row is pasted, where the brief said the validator *names* the row; the human's own vault carries one live `agent-session` binding and meets it first. Debt recorded, not asked: the shell door is `@olai/web`'s `./client/*` wildcard and a curated shell package is a follow-up; chat keeps its three per-directory records on `@olai/state` directly rather than through `Held`. One lane, after phase 6.) The relocation: chat becomes a row with both halves, providing the doors it owns, and nothing chat-shaped remains in core. *Rulings (the human, 2026-09-03, through the question tool): a sibling; chat provides the doors; `agent-session` becomes chat's own prefixed word.* Because phase 6 leaves the session core Effect-shaped, this diff is mostly renames, which is what makes it reviewable at its size. **#490 is closed, not merged** (the human, 2026-09-03): a groundwork draft that moved nothing of chat and whose eight mechanisms split two ways. Its four fixes with value now (`mountBundle` settling, `waiting` naming its missing keys, the `kindWordOf` boot death, kolu and odu tearing down what their applies armed) land first as their own small PR, cherry-picked from the branch. Its four capabilities are cherry-picked by the lane that first uses each: the write fence's core half by phase 6; `Offers`, slot cardinality with the read door, and the validator's migration finding by this lane. The branch stays as the quarry; nothing in it is rewritten from scratch.
   - **What exists (after phase 6).** `@olai/chat` holds sessions as fibers under per-node scopes, the panel's state machine (`chat.ts`), deliveries and their manual scopes (`deliveries.ts`, `scopes.ts`, picks in `@olai/state`'s `wake` record), memory and overheard facts (`memory.ts`, `@olai/state`'s `chat` and `heard` records), probes, and `rosterOf(where, engines)` over the `Agents` registry. Core provides `Agents`, `Deliveries`, `Watching` and `SessionStart` from `openPlugins` with `doorFor: (plugin) => chat?.doorFor(plugin) ?? null`, so a plugin runs without a chat behind a null door. Chat's members are core's: the `chat` and `agents` cells and fourteen `chat.*` procedures in `@olai/surface` (`send`, `resend`, `attach`, `cancel`, `newSession`, `startAgentSession`, `assignSession`, `chooseAgent`, `loadSession`, `reopen`, `sessions`, `answer`, `decline`, `scope`), all on the BROWSER face only (`faces.ts`): the MCP and agent faces carry five core resources and the ops, git and search tools, and no chat member. (An earlier draft of this brief said chat was on the MCP face; #490 read `faces.ts` and corrected it.) `@olai/format/agents.ts` reads the core property `agent-session` to find node agents. The tab's chat is the right panel, the sidebar's agents section, the door on agent rows, the "ask agent" verb, the wake strip, the bar door and the keys, none of which is a slot today.
   - **The wire: a sibling, and it costs nothing on the MCP face.** Chat's surface moves into the plugin's `wire.ts` and composes under `surface/chat/…` like kolu's, with a `faces.browser` map and no `faces.agent` map, exactly as the tenants have. The ruling to accept an MCP rename turned out to have no subject: no chat member is on the MCP or agent face, so no client's tool names or URIs move, and the sibling grammar stays unminted on `surface://` (the negative the vault's debate record ratified twice). Core's own tags stay byte-identical because nothing of chat's remains in core; the root-level mount ask to kolu is not filed.
   - **The doors: chat provides them.** `Agents`, `Deliveries`, `Watching` and `SessionStart` leave `openPlugins` and are provided by the chat plugin's `apply` through `provide` on the host. Every plugin that names one is held `waiting` until chat mounts and unloads when it leaves; that is the paper's rule and the ruling accepts its cost: under `--plugins=kolu` alone, kolu is `waiting`, and the plugins panel says on whose account (the fiber's `PENDING` reading names the missing tag). The engines are `waiting` without chat too, which is correct. If a tenant is later wanted without chat, the escape is an optional need in the bridge (a `Option`-valued tag resolved at use, without reactivity), which this lane does not build. Core keeps `Env`, `Clock`, `Vault`, `Kinds`, `Surfaces`, `Wakes` and `Held`; chat consumes `Vault`, `Held`, `Kinds`, `Surfaces` and a new `Tools` tag core provides for the vault's own MCP tool server, which `serve.ts` hands to `Chat.make` today.
   - **The vocabulary: `chat-agent-session`.** Chat contributes the kind `agent-session`, composed by the existing rule to `chat-agent-session`, and `@olai/format/agents.ts`'s reading moves into the plugin (or takes the composed word as data, the way every other kind does). No new first-party rule. Existing vaults keep their `agent-session` keys by one row in `_olai/Properties.olai`, `{"title":"agent-session","custom":{"type":"chat-agent-session"}}`, which olai never writes for them: the lane documents the row in `docs/plugins/chat.md` and `docs/running.md`, adds it to this vault's Properties in the same lane, and the store's validator names the row when it meets a bare `agent-session` value with no declaration.
   - **The tab: slots the shell declares, then chat's browser half.** Before the move, the shell declares what chat hangs into: `app.panel` (the right panel, `single`), `sidebar.section` (`list`, ordered by bundle rank), `outline.row.door` (`keyed` by kind, the door on an agent row), `outline.row.action` (`list`, the "ask agent" verb), `app.keys` (`list`, shortcuts), and the wake strip and bar door as `app.header` entries. Then `packages/plugins/chat/src/browser.tsx` registers the panel, the agents section, the door, the verb, the keys and the marks' fallback into them, and `packages/web/src/client/chat` and `client/agents` move under it. Core's generic ring-and-dot mark moves with chat, since a mark without a chat is nothing. The furniture chat needs beyond `Clocks`, `Bar`, `Links` and `Wired` is named as it is found and added to `openApp`.
   - **Node agents, already scopes.** Phase 6 left a node agent as a `Scope` and a session as a fiber under it, hanging off `Chat.make`'s scope; here they hang off the plugin's `apply` scope instead, and nothing else about them changes. The write fence's ticket is minted by the plugin now.
   - **The composition root gives it up.** `Chat.make` in `serve.ts`, the `withChat` closure, the `chat` and `agents` cells, the fourteen procedures, `rosterOf`, `whyNoAgent`, the memory default and the 61 sites in `runtime.ts` become the plugin's `apply`. `@olai/acp` stays general. `@olai/state`'s `chat`, `wake` and `heard` records become the plugin's `Held` entries. `olai surface` composes no chat row and stops being a special case.
   - **Done means**: `packages/plugins/chat/` with both halves and one row in `olai.yml` (enabled by default); `@olai/chat` and `client/chat` gone as general packages; `@olai/surface` names no chat member; the browser face lists chat's members under the sibling key and the MCP face is unchanged; `--plugins=` serves the outliner alone with kolu `waiting` and the panel naming why; this vault carries the `chat-agent-session` row and its node agents still resolve.
   - **Not started**: the Unassigned migration UI and the distillation turn (node-agents phase 3); agency (an agent creating child nodes and seating sessions); the Spaces binding; an optional-need helper in the bridge; the orchestrator profile (phase 9), which this lane makes possible by leaving nothing chat-shaped in core.
8. **Loader surface** ([juspay/olai#500](https://github.com/juspay/olai/pull/500), merged 2026-09-05: the switch is in the panel, a flip is session-only, the vocabulary follows the fibers, the pin is on master at kolu#2225's merge commit, and the browser gate is derived from `runtime.roster` per re-compose so gate and group are one generation by construction; one PR, after phase 7). *Rulings (the human, 2026-09-04, through the question tool): the plugins panel gets a switch, overturning the read-only decision; a flip is session-only; no CLI verb against a running serve; `--dump-config` is dropped; out-of-tree plugins wait for phase 10, or never.* A person turns a plugin on or off from the panel, and everything that leans on it follows while the serve runs.
   - **What exists.** `--plugins` is a `disabled` patch over `olai.yml` applied by `@cordisjs/plugin-include` at boot (`packages/bundle/src/bundle.ts`, `mountRows`, settled by `settled` since phase 7); `reportBundle` reads each row's fiber state and a `waiting` row names the tags it is short of (`missing`); the `plugins` cell carries the roster with an `equals`, and the tab follows it through `redial`. The nix home module writes the same flag (`nix/home/module.nix`). The plugins panel (a door in the bar, the human's ask) draws five states per row and is read-only by the earlier decision, which this phase overturns. The kind vocabulary is read once at boot (`packages/server/src/propKinds.ts`, "READ ONCE, at boot, and that is a phase boundary"): the store's codec holds it for the life of the process. The host can already dispose and re-mount a row (`mountPlugin`, `Mounted.dispose`); nothing exposes it. Chat is a row that offers four doors (phase 7), so it is the row whose flip moves the most.
   - **The switch.** One procedure on core's surface, `plugins.set({ id, enabled })` (name it as it is found), on the BROWSER face and nowhere else: `faces.ts` names it there, `faces.test.ts` pins the agent face as an exact set, so an agent cannot turn a plugin off, the same physics `chat.scope` had. The server half is the loader's own verb over the include's patch (`loader.update(id, { disabled })` on the bundle door, or dispose and re-mount through the host, whichever the pin supports cleanly; §8's spike answer on `update` is the starting point). It acts on the running serve and writes nothing: a restart comes back to `olai.yml`, the flag or nix. The panel draws the switch beside the five states; a row that offers doors says which rows go `waiting` when it is off (chat's row names the engines, kolu, odu and the mirror), read off the offers table rather than a list anybody keeps; a row that will not come back shows `failed` with the plugin's own sentence and the switch stays drawn.
   - **What follows a flip, and it is the whole of the phase.** Off: the fiber unwinds every registration (kinds, wake, sibling surface, slots, offers), the sibling leaves the wire, the `plugins` cell moves, the tab redials, dependents go `waiting` naming the tag. On: the reverse, and dependents re-apply. `Held` is keyed by name (phase 7), so a plugin that comes back writes the same record. The reactive half of the paper is what makes this one verb rather than a restart, and this phase is where that claim is benched rather than argued.
   - **8a. The transport serves the live generation** ([juspay/kolu#2225](https://github.com/juspay/kolu/pull/2225), merged 2026-09-04 as `e6c362a`: the served set is one `ServedGenerationSource`, a triple or one thunk of the triple, read in one turn at each accept and gated by `restrictHandlers` per accept; the identity memo proposed in review was dropped in the refactor, so the per-accept walk is upstream's cost until a profile says otherwise). *Ruled 2026-09-04 on juspay/olai#500's finding: the fibers follow a flip and the wire does not.* `serveSurfaceApp` (`@kolu/surface-app/serve`) reads the served set once at bind — `restrictHandlers(group, handlers, expose)` on its first lines, and `options.group` again inside every accept — so every socket accepted after a flip is built over the composition as it stood when the port was bound. A re-mounted sibling's tags route to the retired mount's refusing handler, and a sibling absent at boot has nothing to route at all, because the group is the schema the per-connection `RpcServer` is built over. The composition root already hands live getters (`bound.group`, `bound.handlers`, `faces`); the transport freezes them. **The paper's bar decides it**: a connection is a revertible effect whose dependency is the served set, and confluence says the state after a flip settles must equal a from-scratch load with that configuration, so a socket accepted after the flip must be indistinguishable from one accepted on that boot. Three answers were on the table and two are refused. *C* (refuse first-time enablement, allow off-and-on for rows that booted) makes the failure mode a rule and contradicts the switch-goes-both-ways ruling. *B* (a live-dispatching handler façade in olai) fixes handlers and cannot fix the group or the gate, both read once, and couples olai to which tags are streams, which is the framework's knowledge. *A* is taken, **upstream rather than in olai**: owning the accept in olai would copy back the origin gate, the stale-tab gate, the heartbeat, the header allowlist and the statics that kolu#2137 took out of this tree; the hand-built path the framework names is for drishti's per-host dispatch, which picks *which* runtime serves a socket, and olai's case is one runtime whose served set moves. **The kolu change, in one sentence: `serveSurfaceApp` takes the served set as accessors read at each accept.** `group`, `handlers` and `expose` become thunks (or one `served: () => { group, handlers, expose }`), the per-connection `serveSurfaceSocket` is built over the current generation, and `restrictHandlers` is memoized by the pair's identity so an unchanged generation costs nothing per accept. The upgrade, the gates, the drain and the finalizer do not move, and a value still works wrapped in a thunk, so kolu's own consumers do not change. One small PR against `packages/surface-app/src/serve.ts` and its test; we have permission to file it. **What olai does in return**: the listener passes the getters it already has, and `runtime.ts`'s paragraph promising that reconnect saves an arriving sibling is corrected to what is then true: a connection accepted before a flip is served the old generation until it redials, and the roster cell moving is what makes it redial; existing sockets need no server-side close, because the drop already binds their tags to refusing handlers and the tab's redial is the client half of the revert. **Done means**: the kolu PR is merged and `npins/sources.json` moves to it; the listener hands over accessors; #500's driver reads `padi=connected conn=live stopped=[]` after `kolu OFF, kolu ON`, and the same after switching on a row that was off at boot; `docs/running.md`'s "both ways, including against the flag" is true of the wire and not only the fibers. Until the pin lands, #500 does not merge; the interim C was declined.
   - **8b. The tab follows the roster in place** ([juspay/kolu#2227](https://github.com/juspay/kolu/issues/2227), filed 2026-09-05 by the reviewer on the human's word; kolu, upstream with permission; the browser half of 8a). A roster change is a `redial` today, and a redial hands back a NEW connection with everything the old one handed out dead (`clients`, `core`, `transport`, `readout`, `health`), so `main.tsx` keys the whole app on the wire generation and every piece of local UI state dies with it. That was accepted as rare; the panel's switch and phase 12's approve, edit and stop made it common, and [juspay/olai#510](https://github.com/juspay/olai/pull/510) shows the bill: about fifteen of its fixes hoist one widget's state each out of the tree. The ask is the server's own arrangement mirrored: the connection keeps its identity across a roster move, a sibling arriving appears on `clients`, one leaving refuses in words, and standing subscriptions are re-opened by the connection rather than by the app rebuilding. Done means: `main.tsx` stops keying on the wire generation, a scenario where an editor holding text survives another plugin being switched off, and the hoists in #510 become deletable. Until it lands, the tab's confluence claim is true of the wire and not of the page (§10).
   - **The kind vocabulary follows the fibers.** `propKinds` becomes a live reading: the store's codec asks the `Kinds` registry at validation time (or is told on `changed`), so a row that leaves takes its words with it and a row that arrives brings them. A value under a kind whose plugin is off is plain text, which is what any undeclared key is; no finding. Bench it with a plugin disposed after the store opened.
   - **Done means**: 8a landed and pinned; the switch is in the panel; one scenario flips kolu off and on (its kind leaves and returns, its sibling leaves and returns, the tab follows) and one flips chat off and on (the panel, the agents section, `surface/chat/` and the four doors go and come back, kolu and the engines read `waiting` in between naming the tag); a flip survives no restart and `docs/running.md` says so; the agent face names no plugins verb.
   - **Not in this phase, by ruling**: `--dump-config` (the panel is the table); a CLI verb against a running serve (the panel is the switch, the flag and nix stay the boot-time way); out-of-tree plugins (a nix `extraPlugins` option, phase 10 or never; `olai plugin add` was dropped earlier and stays dropped); persistence of a flip anywhere. **Not started**: HMR (no Bun cache bust exists, §5); a settings file (the configuration ruling of 2026-09-02 puts configuration in `.olai` files, and enablement is the instance's, so there is none).
9. **The journal is a plugin** ([juspay/olai#501](https://github.com/juspay/olai/pull/501), merged 2026-09-05: `dated`, `owed` and the owed index are the plugin's over the derived reading, `day` and `agenda` go through one narrow `Ops.page` door on core's standing cache, no `Vocabulary` tag, and route claims settle once per roster with a collision dropped and reported rather than thrown in the parser. Ruled by the human, 2026-09-05: the `sidebar.entry` placement word is `top | bottom`, the agenda at `top` beside Inbox, accepted for now; it is slot data, so a finer vocabulary later is one edit to the table and its tenants. One PR, after phase 7, in parallel with phase 8.) *Added on the human's word, 2026-09-04.* The day page, the agenda, the calendar, the day's note and the owed badge become a row, `journal`, with both halves; the date fields, the repeat grammar and the overdue predicate stay the format's. What this lane is FOR beyond the row is the shell slot chat did not force: a page with an address, which phases 10 and 11 need and nothing has proved yet.
   - **What exists.** Five things, and only two of them say "daily note". THE FIELDS: `date`, a dated `done`, a dated `cancelled`, `repeat`, validated by `@olai/format` (`dates.ts`, `occasion.ts`, `repeat.ts`, `calendar.ts`), with the spawn-on-done in `@olai/ops`. THE DAY PAGE: `/d/<date>` and `/today` (`routes.ts`, `day/DayPage.tsx`, `day/DayNode.tsx`), a query over every dated node in every outline, riding core's `page` stream as a request kind, with the day's note drawn above it. THE DAY'S NOTE: a `.md` whose basename is an ISO date (`dailyNotePathFor`, `dailyNoteDays` over `documents.keys`), minted by the day page's *+ day note* through an `edit.apply` verb (`server/src/edit.ts`). THE AGENDA: `/agenda` (`agenda/`, `agendaOf` in `format/src/agenda.ts`), and the two owed counts on the sidebar's entry beside Inbox, served by the `owed` stream over an index the ops layer keeps across revisions (`owed.index.test.ts`). THE CALENDAR: the sidebar's month of dots off the `dated` stream (`calendar/`), and *Go to today* and *Go to the agenda* in the palette (`palette/items.ts`). All on the BROWSER face only (`faces.ts`); an agent asking what is late asks `search_nodes` with a date clause. The tab's clock (`clock.ts`, `today.tsx`) tones every row's date badge and is read by the editor's `!` completion, so it is the shell's.
   - **What stays core.** The floor does not move: the four fields and their validation, the repeat grammar and its spawn, `isOverdue` and the badge tone on every row, the `date:` query words, the natural-date completion, the tab's one clock. A journal plugin reads those through `@olai/format`, which a plugin may import; it does not own them. The same split chat kept with `@olai/acp`.
   - **What becomes the row.** `packages/plugins/journal/` with `./wire`, `./server`, `./browser`, `./testids`, `./all.css`, and one row in `olai.yml` (enabled by default). The server half computes four readings off the `Vault` door's derived reading with the format's own functions: which days of a month are lit, the two owed counts, one day's rows and note, the agenda's three stretches. The owed index moves with it and is kept across revisions the way kolu keeps its claims. The browser half is `agenda/`, `calendar/`, `day/` and the two palette entries, moved.
   - **The wire: a sibling, browser face only.** `surface/journal/…` with four streams: `dated` and `owed` as they are today, plus `day` and `agenda`, which leave core's `page` stream (they are the plugin's readings, not the tab's reading of a file). One procedure, `note.mint({ date })`, replacing the `edit.apply` verb. No cells, no agent map. Core's `page` stream keeps its file and node kinds only; `faces.ts` loses the two streams.
   - **The shell grows three slots, and one door widens.** These are the lane's real cost and its point. `app.route` (list): a page kind with an address grammar, a breadcrumb and a face, read by `routes.ts`, `page.ts` and `PageView.tsx`, so `/today`, `/d/<date>` and `/agenda` are the plugin's; core keeps the parser's box, the pane and the filter bar. `sidebar.entry` (list, placed): the agenda entry sits beside Inbox above the month by the 2026-08-19 ruling on what comes ahead of the journal's questions, and `sidebar.section` hangs below the app's own, so either this slot takes a placement word (`top`, `below`) or the ruling moves; the lane proposes the word and the human rules. `app.palette` (list): a navigation item with a label, a hint, search words and a route, which `app.command` (a prefix and a line) is the wrong shape for. The `Ops` door from phase 7 writes one property on one node; minting a document is its second write shape, as narrow as that.
   - **Not a kind.** The journal contributes no property kind: `date` is the format's, not a plugin's word. So phase 8's live vocabulary does not touch this lane, and the fence's `NOT_A_PLUGIN` table gains a `journal` row only where core's own prose spells the word (`docs`, `Sidebar.tsx`'s header, `format.md`), recorded as an equality like `chat`'s.
   - **Done means**: `packages/plugins/journal/` with both halves and one row; `agenda/`, `calendar/` and `day/` gone from `@olai/web`; `@olai/surface` names no `dated`, `owed`, `day` or `agenda` member; `--plugins=` minus `journal` serves an outliner with no day page, no agenda, no calendar, no owed badge and no *Go to today*, in one scenario; the three slots have a reader and the route slot has one tenant; the existing day, agenda and calendar scenarios pass unchanged against the sibling.
   - **In parallel with phase 8, and where the two meet.** The two lanes touch different files: phase 8 is the plugins panel, `propKinds`, the loader's update and a `plugins.*` procedure; this lane is the shell's routes, sidebar, palette, the page stream and a new package. They meet at `olai.yml` (one appended row each), `faces.ts` (one adds a procedure, one removes two streams), and `fence.test.ts`'s tables (one row each). Whichever merges second rebases across those three; neither needs the other's mechanism. After both are in, one scenario flips `journal` off and on through phase 8's switch and belongs to whichever lane is still open, or a two-line follow-up.
   - **Not started**: a journal on the MCP face (an agent has `search_nodes`); a second tenant of the route slot (phase 18's outline view, or a dynamic plugin's page in phase 12); the `sidebar.entry` placement ruling, which the lane proposes and the human decides.
10. **The ledger is a plugin** ([juspay/olai#502](https://github.com/juspay/olai/pull/502), merged 2026-09-05: row config landed as the general mechanism (`Row.config`, `definePlugin({ config })` validated at load as a Standard Schema, `--commit`/`--push` a patch on the git row, drawn under the row on the plugins panel), `Pin` deleted, and the MCP tools call through the ledger door with the face's writer with no sibling-key matching left in core. *Ruled by the human, 2026-09-05, through the question tool: agents lose the two MCP reads (`surface://cells/git` and `surface://cells/pending` leave the MCP face; the `commit` and `push` tool names stay; no kolu ask); the flags become row config in this PR (a row's `config:` in `olai.yml`, validated against a `Config` schema the plugin exports, with `--commit`/`--push` a CLI patch onto the git row and `Pin` deleted); and a plugin's configuration is shown on the plugins panel under its row, read-only here, editable in phase 11.* One PR, after phase 7, in parallel with phases 8 and 9.) *Added on the human's word, 2026-09-04, with the orchestrator profile dropped from the plan the same day.* Git becomes a row, `git`, providing a `Ledger` door that core defines and nothing else provides today. `GIT_OFF` stops being a mode and becomes "no provider mounted".
   - **What exists.** `@olai/git` (1,073 lines: the repository reading, commit, push, the resume of a stopped loop); the ledger's ops-side half in `@olai/ops` (`pending.ts` 1,411 and `committed.ts` 281: what is waiting to be recorded, the writer trailers, the quiet-window commit, `fixedPolicy` over the `--pin` flag's `GitPin`); on the wire the `git` and `pending` cells and `git.commit`, `git.push`, `git.resume` procedures; `gitWiring` and the two connectors in `runtime.ts`; the pill, the mobile banner and the commit panel in the tab (`web/src/client/commit/`, 2,009 lines, mounted from `AppHeader.tsx`); `docs/git.md`. The `chat-agent` writer word is the format's and stays (the fence's `NOT_A_PLUGIN` row says why).
   - **The one ruling, and it gates search too.** Git is the first promotion whose members are on the AGENT and MCP faces: `git.commit` and `git.push` are tools an agent calls, and the `git` and `pending` cells are resources. Chat and the journal were browser-only, so a sibling cost nothing there. A sibling here renames two tools and two resource URIs, and the vault's debate record ratified the negative on minting sibling grammar on `surface://` twice. Two ways: (a) accept the rename on the agent face (tools are discovered by listing, so no `.mcp.json` moves; `docs/mcp.md` and any prompt that spells `git.commit` do); (b) file the root-level mount ask to kolu (unprefixed members merged into the root spec, collisions refused, the twin of the sibling mount kolu#2223 took) and wait for it. **Decision for the human.** The lane recommends (b) filed now, because phase 13 (search) and every later core member on the agent face meet the same question, and one framework ask answers all of them; if kolu's answer is slow, (a) is a fallback that costs a doc and a prompt.
   - **What stays core.** The write gate: a write still lands on disk through `@olai/ops` and WAITS to be recorded, and "what is pending" is a question about the served files against a history, which is the ledger's reading. So `Ledger` is a core door with three members (what is pending, record these with this writer, push) and no provider in core; `ops.commit` and `ops.push` become calls through the door and refuse in words when nobody stands behind it, which is what `GIT_OFF` said with a mode. `committing.ts`'s writer words stay the format's.
   - **What becomes the row.** `packages/plugins/git/` with both halves: the server half is `@olai/git` plus `pending.ts` and `committed.ts` moved, offering `Ledger` (a fifth entry in `OFFERABLE`); the `--pin` flag becomes the row's `config` in `olai.yml`, which the loader's entries already carry, and the CLI writes it as a patch the way `--plugins` is one. The browser half is `commit/` moved, the pill registered into `app.header` (one seat per plugin, and this row's one), the banner and the panel travelling with it as chat's strip did.
   - **The wire.** Under the ruling's answer: `surface/git/…` with the two cells and three procedures on browser, agent and MCP maps, or unprefixed under the root mount. Either way `@olai/surface` names no git member and `faces.ts` loses five rows.
   - **In parallel with 8 and 9, and where they meet.** Different files from both: phase 8 is the plugins panel, `propKinds` and the loader's update; phase 9 is routes, sidebar, palette and the page stream; this is `git`, `ops`' two files, the commit chrome and `gitWiring`. They meet at `olai.yml` (a row each), `faces.ts` (rows added and removed in different regions), the fence tables, and `runtime.ts`, where phase 8 edits the plugins cell and this lane removes the git connectors: the one file to rebase with care. The `app.header` slot already has one tenant (chat's toggle), so the pill is its second and proves the seat.
   - **Done means**: `packages/plugins/git/` with both halves and one row (enabled by default); `@olai/git` gone as a general package and `ops` holds no git; `@olai/surface` names no git member; `--plugins=` minus `git` serves a vault whose writes land and are recorded by nobody, the pill absent and `ops.commit` refusing in words, in one scenario; the existing commit, push and resume scenarios pass unchanged; `docs/git.md` and `docs/running.md` say the row and its `pin` config.
   - **Not started**: a second ledger provider (the reason the door exists; nothing needs one yet); the shell's `app.banner` seat for the mobile banner, if the panel slot cannot carry it.
11. **Plugin settings** (one PR, after phase 10; the second layer of plugin configuration, in DeepSeek Harness's shape). *DROPPED by the human, 2026-09-04, and [#509](https://github.com/juspay/olai/pull/509) closed unmerged. The thing wanted, editing kolu's knobs without a restart, already worked: they are properties on the `watch` node of `_olai/Kolu.olai`, edited in the outliner, read live by kolu, refused in kolu's own sentence when malformed, and in git with the vault. Phase 11 added a form, a schema, a badge and a general service for one tenant with three duration strings, at the price of a core service, a cell, a procedure, a file convention and about thirteen hundred lines. The rule that stands instead is the precedent: a plugin with knobs reads them from its own node in `_olai/<Plugin>.olai`, the outliner is the editor, the plugin owns the sentence for a bad value, and nothing secret goes in a vault file (the secret-field ruling of the same day). Revive from the branch when a second plugin actually needs a form. The rulings below (one file, kolu first, no migration, secrets refused) are kept as the record of what was decided.* *Added on the human's word, 2026-09-05, with three rulings: row config lands in phase 10, this layer is its own phase, and a plugin's configuration is shown on the plugins panel under its row.* Phase 10 gives a plugin what a deployment runs it with; this phase gives a person what they may change while it runs, drawn where a plugin's other facts already are.
   - **What exists (after phase 10).** A row in `olai.yml` may carry `config:`, validated at load against the `Config` schema the plugin exports beside its `apply` (defaults on the fields, an invalid config fails the load with a sentence); a CLI flag is a patch onto a row's config, as `--plugins` is a patch onto `disabled`; the plugins panel draws each row's config read-only under the row. Behaviour a vault decides already has a home: `_olai/Kolu.olai` is kolu's file and travels with the vault, under the 2026-09-02 ruling that configuration lives in `.olai` files. Enablement stays the instance's (phase 8).
   - **dsh's model, which this copies.** A `settings` service holds one document with a section per plugin, keyed by the plugin's name; a plugin registers its section with a schema and the subset of its row config as the base layer; the resolved value is schema defaults, then the row's config, then the user's edits; a field may be `applies: restart` (the page badges the change as pending); a `validate` hook refuses a write the schema cannot judge; writes to one section are serialized and observers are told after each commit. The settings page has a Plugins tab with one card per served section, each registered by its plugin into a slot keyed by its name.
   - **The shape here.** A `Settings` core service: `register(schema, base, { validate, applies })` on the calling fiber (stamped by name, released with the scope), `get`, `watch`, `update`, `unset`. The document's home is **ruled by the human, 2026-09-04, through the question tool: the vault, one file, `_olai/Settings.olai`**, one node per plugin keyed by the plugin's name, its properties the section's fields, travelling with the directory, committed by the ledger like any write, and judged by the validator against the plugin's schema the way `_olai/Kolu.olai`'s node is today. Not one file per plugin, and not the machine's state home (`LocalState` stays for what nothing could rebuild). Host facts (paths, secrets, policy) stay on the row. The UI is the plugins panel: the read-only config phase 10 draws becomes the base layer shown beside editable fields the plugin's schema describes, rendered by core from the schema with no plugin words in core, and a plugin that wants its own face registers it into a keyed slot (`plugins.row.settings`) as dsh's cards do. The wire is one core cell per plugin section plus one procedure (`settings.set`), browser face only.
   - **First tenant, ruled by the human, 2026-09-04: kolu's watch knobs, and they move.** `heartbeat`, `held-for` and `nag` (today custom properties on the `watch` node of `_olai/Kolu.olai`, read live by kolu's watcher) become kolu's registered section with a schema, stored in `_olai/Settings.olai` like every other plugin's, and drawn as editable fields under kolu's row. No migration, on the human's word (2026-09-04, no backwards compatibility yet): kolu stops reading `_olai/Kolu.olai`'s `watch` node, a vault with no kolu node in `_olai/Settings.olai` runs on the schema's defaults, and the person re-enters the three values on the panel once. The old node is left alone and `docs/running.md` says which file kolu reads now. The `mutes` node in `Kolu.olai` is already empty by design and is not a setting. Chat's per-directory records stay machine-local (phase 15). Git's `commit`/`push` policy stays row config by the human's earlier ruling (the instance's, no runtime door) and is shown, not edited.
   - **Done means**: `Settings` exists with one tenant; a section's fields are edited under the plugin's row on the plugins panel and the plugin observes the change without a restart; a `restart` field is badged; a schema with a `secret` field is refused at register with a sentence pointing at the row (ruled 2026-09-04; the vault is in git, so no secret may be written through this door); the document's home is ruled and documented in `docs/running.md`.
   - **Not started**: per-vault enablement (ruled out: the vault may not decide what the host runs); a settings page separate from the plugins panel (ruled out for now: one place per plugin).
12. **Dynamic plugins** (one PR, after phases 8 and 9: the switch and the route slot). *[#506](https://github.com/juspay/olai/pull/506), merged 2026-09-04 as `bc827db0`, with the four items below landed: the three verbs are MCP tools with a route test, the chip example reads `context.entry.value`, the stray file is gone, and `a_plugin_the_vault_defines.feature` covers the lane. A node agent's field report on 2026-09-04 (it wrote the doc's example on the branch and it mounted) found what the review missed: the three agent verbs are on the face but not in the MCP tool table, so no agent can call them; the doc's chip example reads `props.value` where the slot hands `context.entry.value`; a stray zero-byte file at the repo root; no feature file. Follow-ups recorded under "not started": the typing vocabulary gaining the vault's own definitions as a third source beside `built` and `enabled`, so a vault can point a key of its choosing at a dynamic kind; and, ruled doc-only by the human the same day, the fence covering only writes through olai's door while every shipped engine has a shell on the same host. Three departures accepted: no `plugins.define`/`undefine`, a definition is two notes written through the ordinary door; `mountPlugin` rather than a loader row; a vault may not take a built word. The review's blocking finding, that `approved` was an open property the agent's own `set_prop` could write, is closed at the session ticket: the fence's forbidden table now carries `approved` with its own clause, judged on every record a plan changes. A dynamic row's state is read off the same report the built rows use, re-read before every re-compose. The tab's render-failure report-back is recorded in `docs/dynamic-plugins.md` as not done, the same gap built rows have.* A node agent writes a plugin into its subtree, a person approves it, olai mounts it while serving, and retract is dispose. *Out-of-tree plugins built from an npins pin (a nix `extraPlugins` option and a row appended at build) were moved here from phase 8 by the human on 2026-09-04, to land when there is a second plugin to build from a pin, or never.* The paper's stated next validation, and dsh's `cordis-host-runner` (`cordis_define` / `cordis_run` / `cordis_inspect`) is the reference.
   - **What exists.** Every mechanism: a row is a module specifier the loader `import()`s through `resolve` (`packages/effect-cordis/src/loader.ts`), and Bun imports TypeScript from disk; a plugin is `definePlugin` over Effect with `needs`; `mountPlugin` and `Mounted.dispose` mount and unmount one; the roster cell moves and the tab redials; the plugins panel is where a person looks. What does not exist: a place for the source, an approval, and a browser half that was not built.
   - **Where the source lives.** In the vault, under the node agent's subtree, as the configuration ruling requires: a node with a `plugin` property whose subtree holds `server.ts` and, optionally, `browser.tsx`, written by the agent through the ordinary vault write door (so the subtree fence of phase 6 applies, and so the plugin is versioned by the ledger like any other file). The row is derived: `id` is the node's word, `name` is the file's path.
   - **The tools (agent face).** *Superseded by #506, accepted by the human 2026-09-04: there is no `plugins.define` or `plugins.undefine`; a definition is two notes written with `add_node`, `set_desc` and `set_prop` through the ordinary door, and the three that remain are `plugins.inspect`, `plugins.run` and `plugins.stop`. Kept as the proposal.* `plugins.define` writes or replaces the files and returns the row's identity and the version (a content hash); `plugins.run` requests activation of one version; `plugins.inspect` answers what a plugin may name (the tags, the slot catalog with cardinality and current occupants, the roster) so the agent reads the registry before writing code; `plugins.stop` and `plugins.undefine` retract. Each is a member on core's surface exposed to the agent face, and each is a durable fact in the conversation the way a tool call is.
   - **The approval (browser face).** A `plugins.run` from an agent lands as a pending row in the plugins panel with the source visible; a person approves once, or approves the plugin for later versions too; the panel is where the read-only rule gets its one verb, because this is the case where the fact being decided is a person's, not the instance's. Until approved, nothing mounts. This is the paper's §6.3 read honestly: the code runs with the process's authority, so approval by the owner is the boundary, and sandboxing is out of scope.
   - **Mounting.** *Superseded by #506, accepted by the human 2026-09-04: the server half is `mountPlugin` on the same host as the rows, not a loader row, because a row would be a second identity and an `olai.yml` this phase may not write. Kept as the proposal.* Server half: a row appended to the live bundle (`loader.update` from phase 8), so it is a fiber like any other, with the same states and the same containment. Browser half: the server builds the file with `Bun.build` into a chunk under `/_olai/plugins/<id>-<hash>.js` and the roster cell carries the chunk URL beside the row; the tab's `BROWSER_ROWS` becomes the generated table joined with the dynamic rows from the cell, and the existing redial mounts it. A version switch is dispose-then-mount; a render failure in the tab is reported back and the row shows it.
   - **Rulings (the human, 2026-09-05, through the question tool).** *Imports*: an agent-written plugin is ordinary plugin source and imports `@olai/plugin-api`, `effect` and `solid-js` by their bare names; olai resolves them itself, the loader's `resolve` mapping the server half's imports to the modules inside the running binary and the `Bun.build` for the browser half aliasing the same three, and any other bare import is refused with a sentence naming it (a vault has no `node_modules` and never will). *Approval*: recorded in the vault, next to the plugin, as a property on the plugin's node (`approved: <content hash>` for one version, `approved: always` for every later one), so it travels with the vault and is versioned by the ledger like the source, under the 2026-09-02 ruling that configuration lives in `.olai` files; the panel's approve verb writes that property through the ordinary write door, and a version whose hash the property does not name is pending again. Runs beside #502; they meet on the plugins panel (approval rows against config rows), and whoever merges second rebases that file.
   - **Done means**: an agent defines a dressing for a kind it claims into its subtree, the person approves, the chip appears in the tab without a reload, `undefine` removes it and the chip is gone; a failed server half lands `failed` with the plugin's sentence and touches no sibling; every define is a ledger commit like any vault write.
   - **Fixed, 2026-09-05**: a trashed definition was still read as a definition, because `definedIn` walked every regular node including those in `_olai/Trash.olai`. [juspay/olai#525](https://github.com/juspay/olai/issues/525), fixed by [#526](https://github.com/juspay/olai/pull/526) merged as `8b37e8aa`: `isPutAway` in the reader, a live namesake is no collision with a trashed one, the next `follow` disposes a mounted one, the approve verb refuses a node in the trash, and a scenario.
   - **Not started**: sandboxing or capability attenuation for agent-written code (the paper's `intercept` is the seam, and phase 6's write fence is the first use of it); publishing a dynamic plugin as a package; a `Search` provider written by an agent, which is phase 13's door and this phase's natural second tenant; the typing vocabulary gaining the vault's own definitions as a third source beside `built` and `enabled` (a definition travels with the vault, so the same-on-every-host argument that judges `type:` against `built` holds for it, and `colour: swatch-hex` becomes reachable). *Ruled by the human, 2026-09-04: the fence covers writes through olai's door and the vault is the owner's directory; an engine with a shell on the same host is outside what olai polices, so no witness and no signing, one doc sentence.*


12a. **The bridge owns provision, teardown order and cancellation** (one PR, before phases 13 and 14, which each add a door on the same path; runs beside 11, which adds none). *[#507](https://github.com/juspay/olai/pull/507), merged 2026-09-04 as `57af448b`, the duplicate-provider sentence restored in olai's words: every done-means has a behavioural test, provision is on the offering fiber, the teardown order is bridge-owned with the `_disposables` coupling named in the README, withdrawal leaves `waiting` and recovers. One small ask, the duplicate-provider sentence back in olai's words naming both rows.* *Added on the human's approval, 2026-09-04, from the review in [juspay/oss.olai#1](https://github.com/juspay/oss.olai/issues/1) (findings 1 and 5 and the three qualifications), verified against the pinned Cordis `fiber.ts` and `reflect.ts`.*
   - **What is wrong.** (1) `Offers.offer` provides on the host, which is the root Cordis context (`packages/effect-cordis/src/host.ts`, `ctxOf(host).provide`). Cordis records the providing fiber on a service and withholds it until that fiber is ACTIVE; the root always is, so the moment chat's `apply` offers `Agents`, every fiber that names it activates, before chat has finished and whether or not it then fails. (2) `definePlugin.apply` awaits the whole initialisation before handing Cordis a disposer, and Cordis has no hook into a running `apply` (`_setEpoch` records the inactive epoch and returns while `inertia` is set; `_reload` awaits `_execute` first), so a plugin whose `apply` never settles cannot be stopped by a flip, a stop or a shutdown, and `openHost` has no close at all. An agent-written server half (phase 12) is exactly the code that will hang.
   - **Provide on the offering fiber.** `offer` provides on the offering plugin's own context, carried into the Effect environment by the bridge (it already holds `ctx` inside `apply`). Cordis then withholds the service until the provider is ACTIVE, drops consumers when it leaves, and refuses a second provider itself, so the bridge's own duplicate claim goes.
   - **Teardown order is the bridge's to own.** A `ctx.provide` made during `apply` is a Cordis effect of its own in the fiber's disposer set, which `_unload` runs with `Promise.all` beside the scope close; and the disposer Cordis hands back is guarded, so a second caller returns at once without awaiting the revoke the first started. Registering the revoke as an Effect finalizer as well is therefore not enough. The contract to state and test: dependent cleanup finishes while the resources it needs still exist, then those resources are released.
   - **Cancellation while LOADING, with readiness kept.** The initialisation runs as an Effect fiber the bridge keeps by Cordis fiber; `apply` still awaits it, so a fiber is ACTIVE only after its work is done. The interrupt is reached from the bridge's own dispose wrappers (flip, stop, host close) and from Cordis's `internal/service` event for a dependency withdrawn while loading. On interruption `apply` unwinds the scope and *resolves*, not rejects: a rejection lands as `_error` and the row would read `failed` after an interruption that was nobody's fault. The two causes end differently, and both are the runtime's own words: an explicit stop disposes the fiber and the row reads `off`; a dependency withdrawn keeps the fiber, Cordis holds it `PENDING`, olai reports `waiting` naming the missing key, and when the dependency returns the initialisation runs again and the row reads `running`. Disposing on withdrawal would throw away the reactive recovery this adoption exists for. Cooperative limits are documented: an interrupt lands at the next yield, and synchronous JavaScript is not preempted.
   - **Host close.** `openHost` gains a close that disposes every mounted fiber, including one with empty `needs` that acquired a resource; `serve` and the tests use it.
   - **Done means**, each a test in `packages/effect-cordis`: an offer followed by delayed initialisation leaves consumers pending until the provider is ACTIVE; an offer followed by a failed initialisation activates no consumer; provider replacement and removal with asynchronous dependent cleanup, where a dependent finalizer *uses* the provider's resource and the order is asserted, not the row states; an interruptible initialiser that acquires a resource, offers a service and then waits forever: its consumer stays pending; an explicit stop ends the initialisation, releases the resource and leaves the row `off`; a dependency withdrawn ends the initialisation, releases the resource and leaves the row `waiting` with the missing key named, and when the dependency is provided again the initialisation runs again and the row reads `running` with the resource re-acquired; host close during initialisation, with outstanding background work, and with asynchronous cleanup. The plugin switch and restart scenarios pass unchanged.
   - **Not started**: preemption of synchronous code (impossible in-process); a per-plugin timeout on initialisation (a policy, and a later ruling if a hung dynamic plugin turns out to be common).

12b. **Plugin-owned service keys** *([#523](https://github.com/juspay/olai/pull/523), merged 2026-09-05 as `baba8feb`: `own` stamps the key from the fiber, `offer` is a membership check on `OFFERABLE` again with no owner map and no fence exception, `serviceKeys` is the catalog `plugins.inspect` advertises, and the two-dynamic-plugin scenario proves the lifecycle; no built tenant yet, the mirror lane is the first)* (one PR; 12a is merged; the open form of §4.2, deliberately limited since phase 7). *Approved in principle by the human, 2026-09-04, on the reviewer's finding 2 and the same resolution proposed by both reviewers: protect core's keys, permit plugin-owned ones.* A plugin may offer a key of its own, stamped `<plugin>.<word>` from the fiber's name the way a kind word is (`swatch-hex`), never from the caller; another plugin names it in `needs` and waits, activates, and unloads by Cordis's own rule; the SET of core-defined door keys (`OFFERABLE`) stays closed, so a plugin may offer only one of core's doors or its own namespaced word, and which row stands behind a core door is not core's to say: any row may, one at a time, and Cordis refuses the second naming the first. *(Reworded 2026-09-05 after #523 read the earlier "closed to everyone but their rows" as core reserving each door to a named row, which would end the second-provider design of phases 13 and 14 and make core spell plugin names.)* First tenant: the mirror lane (kolu's fleet and odu's runs, the edge §4.2 was written for). Done means: two plugins, one providing and one consuming, with no edit to core's allowlist, benched through pending, activation, removal and replacement; `plugins.inspect` (phase 12) lists plugin-owned keys beside core's so an agent-written plugin can name one.

12c. **Every cross-plugin edge is a door** (one PR, after 12b merges; runs beside 16 only if it stays out of `serve.ts`, else after). *Added 2026-09-05 on the human's word, after a survey of master for dependencies that go around Cordis rather than through it.* The survey found four, and one that is fine.
   - **xyne-spaces imports chat.** `packages/plugins/xyne-spaces/src/config.ts` imports `SESSION_TYPE` from `olai-plugin-chat/binding`, the kind word chat teaches for a node's seating, and `olai-plugin-chat` is in xyne-spaces' `package.json`. The file argues the import is deliberate, and the argument was right before 12b: there was no door to name. Now there is. Chat offers `chat.seating` with `Offers.own`: over a reading, which node is seated by which session, the fold `SESSIONS` does today. xyne-spaces names it in `needs`, drops the import and the dependency, and gets what an import never gave it: it waits for chat, unloads when chat leaves, and comes back when chat does. This is the first shipped tenant of `own`.
   - **Chat imports identity.** `packages/plugins/chat/src/browser/chat/Speaker.tsx` imports `whoAmI`, `saying` and `UserIcon` from `olai-plugin-identity/person`, and `olai-plugin-identity` is in chat's `package.json`. What crosses is a rendering kit over core's own `who.get`, not identity's reading, so it is core furniture that happens to live in a plugin. It moves to `@olai/web` (or the plugin API's browser door), the way the shortlist kit stayed core in phase 13, and both packages drop the dependency. Browser halves have no `own`, and this needs none.
   - **Core names chat and xyne-spaces in `localState.ts`.** The `legacyFor` table from phase 15 migrates the old `chat/`, `wake/`, `heard/`, `hold/` and `mirror/` layouts by plugin name, with an expiry note. The human ruled no backwards compatibility on 2026-09-04, so the table and the old-layout reads are deleted; a fresh start writes the new layout.
   - **Core writes as `"chat-agent"`.** `serve.ts` hands the MCP face a writer named `chat-agent` (`writerAt(…, { writer: "chat-agent" })`, `ops.commit(request, "chat-agent")`), a plugin's word in the composition root, from before the face was core's. The writer becomes the face's own word (`mcp`, which after phase 16 is the row's name); a node session keeps writing through its ticket under its own name. The ledger's attribution and the doc line that names the writer follow.
   - **A file named for what left it.** `packages/server/src/identity.ts` held the identity implementation until #512 and now holds core's side of the door only: `NOBODY`, the per-connection `CurrentWho`, `readingOf` and `asWho`, and the `GET /olai/who` route. The name says plugin and the contents say core, so a reader asks why it is still there (the human did, 2026-09-05). It is renamed for what it is, `who.ts` or `viewer.ts`, with its four importers following.
   - **Fine as it is:** `gitPolicy.ts` patches the row `git` from `--commit`/`--push`. The composition root may name rows, as `olai.yml` and the profiles do; a deployment flag onto a row's config is phase 10's ruling, not a reach-around.
   - **Done means**: no plugin's `package.json` lists another plugin; `fence.test.ts` holds that as an equality with no exceptions table for it; core spells a plugin's name only in the bundle table, `olai.yml`, the profiles and `gitPolicy.ts`; xyne-spaces' row reads `waiting` with `chat.seating` named when chat is switched off, and recovers when it is switched on, in a scenario.

12d. **Journal offers its readings, and an agent writes the daily brief** (one PR after 12b, one after that; the second is the worked example `docs/dynamic-plugins.md` deserves). *Added 2026-09-05; the human chose it from three candidates.* The journal plugin already computes what is dated today and what is owed (`packages/plugins/journal/src/readings.ts`), but only the day and agenda pages can reach it.
   - **Journal offers `journal.agenda`** with `Offers.own`: given a date and the reading it is about, the dated and the owed nodes as located rows, the same shape the agenda page draws. The reading comes in, as with `Search`, so the answer is about one snapshot.
   - **The daily brief is a vault-defined plugin.** A node agent writes it into its own subtree: `needs: [Clock, Deliveries, serviceTag("journal.agenda")]`, and each morning at an hour the node's property names it reads the agenda and delivers a short brief into that node's conversation through `Deliveries`, the way kolu's doorbell delivers a ring. The person approves it once on the panel. It is written as the worked example in `docs/dynamic-plugins.md`, replacing the swatch as the example that does something, and `plugins.inspect` is how the agent found the key.
   - **Done means**: `journal.agenda` is on `plugins.inspect` while journal runs and off it when journal is switched off; the brief's row reads `waiting` with the key named while journal is off and `running` when it returns; a scenario approves the brief and reads one delivery in the node's conversation; the doc's example compiles through `buildHalf` in a test, the way the swatch does.

**The promotion track (phases 13 to 18).** *Approved by the human on 2026-09-04: §9's "olai as a Cordis application" stops being optional and is the plan's second half.* Each phase below promotes one part of the root to a row by the same skeleton phases 7, 9 and 10 use: what exists, what stays core, what becomes the row, the wire, the slots or doors the shell has to grow first, what it runs in parallel with, done means. The order is by what each unblocks and by size, smallest first; §9's table says why each earns a fiber and what to leave alone. Every one of them is a sibling or a root-mounted contribution under phase 10's ruling.

| Phase | Part | Lines today | Reason (§9) | Becomes | Needs first | Parallel with |
| --- | --- | --- | --- | --- | --- | --- |
| 13 | Search (`@olai/index`, `search.nodes`, the tab's `search/`) | 533 + 1,600 | 2 | `Search` door in core, the matcher as its provider row | phase 10's ruling (`search.nodes` is on the agent face) | 11, 13 |
| 14 | Identity (`@olai/identity`, `who`, the tab's `who/`) | 429 + 314 | 2 | `Identity` door, tailscale as one provider row | nothing | 11, 12 |
| 15 | Machine-local state (`@olai/state`, `LocalState`; merged, #505) | ~500 | 1, 4 | one `LocalState` door (renamed from `Held`) every row keeps its record on, one file per plugin; chat's three records merge onto it (phase 7's debt) | nothing | #502, 12, 13, 14 |
| 16 | Transports and profiles (`listener.ts`, `mcp/`, the faces) | 228 + 1,142 | 2, 3 | `ws` and `mcp` as transport rows; `olai web` and `olai surface` as two row lists over one base bundle | 14 | none (touches `serve.ts` whole) |
| 17 | The vault as a row (`store` mounted from a row; `Vault` already exists) | part of `store` | 1 | the test-minimal profile: vault plus kinds and nothing else | 15 | none |
| 18 | The shell's own faces (outline view, preferences, the core dressings for `date`, `path`, `doc`, `ref`) | most of `web` | 3, 4 | client plugins registering into the same slots the tenants use | 9's three slots, 15 | none |

13. **Search is a plugin.** *[#516](https://github.com/juspay/olai/pull/516), merged 2026-09-05 as `588e72bd`, with `search_is_a_plugin.feature` (the row off, and the switch off and back on with a fresh table finding the same node): the `Search` door is the seventh `OFFERABLE` key read per ask, the row has no `Vault` need and levels its table against the reading it is handed, the index is an `acquireRelease` on the fiber, absence answers on `refusals` in words, and the scope ruling is kept exactly.* `@olai/index` and the matcher become `packages/plugins/search/`, with `HeaderSearch.tsx` as the plugin's browser half on the `app.header` slot (*scoped by the reviewer on the agent's question, 2026-09-04: the tab's `search/` shortlist kit, `Shortlist`, `Result`, `nodes`, `cursor`, `props`, `place`, is core furniture four core doors share and stays in core; with the row off the header has no box, and the palette, the composer's `@` list, the edges panel and the move widget each get the server's refusal in words. Growing those four doors into slots is phase 18's*); core defines a `Search` door (query in, located nodes out, over the derived reading) and `search.nodes` calls through it; the plugin offers the matcher. The parked semantic recall (#165) is then a second provider row with no core PR. Done means: `--plugins=` minus `search` serves a vault where the palette's search and `search.nodes` refuse in words; the search scenarios pass unchanged. 12a is merged; runs beside 11, 12b and 14; it is a `Search` provider, and a dynamic plugin (phase 12) could be one too. *Scope ruled by the human, 2026-09-04: just the move. The parked semantic recall (#165) stays parked as a later provider row and is not in this PR.*
14. **Identity is a plugin.** *[#512](https://github.com/juspay/olai/pull/512), merged 2026-09-04 as `367642b5`, with the flip test and `Identity` in the inspect catalog landed first. The door is the sixth `OFFERABLE` key, read per call by the three readers with `NOBODY` behind no row; the chip hangs in a new `app.viewer` slot keyed by the app; with the row off there is no chip rather than an anonymous one (a departure from "said in the chip", accepted: absent means absent). Two departures for the human: the `OLAI_IDENTITY_*` family stays environment read through `Env` rather than row `config:` (the PR's reason, proxy wiring is set where the proxy is and should not have two authors), and chat imports the chip's rendering kit from `olai-plugin-identity/person`, a library edge beside the xyne-to-chat one, no fiber coupling. One seam, named in the PR and not blocking: the socket's header allowlist is fixed at the bind, so a row switched on mid-serve names its headers at the next start; that is 14a below.* `@olai/identity` and the tab's `who/` become `packages/plugins/identity/`; core defines an `Identity` door (who is this request, from these headers) read by the listener and the capture attribution; the plugin offers the tailscale reading. Done means: minus `identity`, every request is nobody and a capture records no login, said in the chip; the identity scenarios pass unchanged. 12a is merged; runs beside 11, 12b and 13.
   - **14a. The upgrade's header allowlist follows the live row** (*kolu side: [juspay/kolu#2229](https://github.com/juspay/kolu/pull/2229), merged 2026-09-05 as kolu `60ecf0da4`: a thunk read per accept, a bad live list serves the socket anonymously with `UpgradeHeadersRefused`, `checkUpgradeHeaders` exported for the mint site; the olai side is [juspay/olai#528](https://github.com/juspay/olai/pull/528), reviewed good to merge 2026-09-05 once re-pinned to master: one `currentIdentity` read per accept and per request, the boot check with the framework's own grammar under the bind's `onError`, both report arms with a `never` default, the flip and typo tests and the switch-on scenario; a loader-path unhandled rejection the PR routes around is to be filed*) (kolu, upstream; 14 is merged, so this is open; the same class as 8a and the same ruling, upstream with permission). `@kolu/surface-app`'s `serveSurfaceApp` reads `upgradeHeaders` once at the bind, so a serve that came up without the identity row and switched it on at the panel answers `GET /olai/who` and `/mcp` at once while every open and every new socket stays anonymous until a restart: the state after the flip is not the state of a from-scratch load with the row on, which is the confluence gap #2225 closed for the served set. The change is the one #2225 made for `group`/`handlers`/`expose`: `upgradeHeaders` becomes readable per accept through the same `ServedGenerationSource` (or a thunk beside it), olai's listener passes `() => identity().headers`, and the sentence in `packages/plugins/identity/docs.md` and `@olai/plugin-api`'s `Identity` comes out. Done means: the kolu pin moves, and a unit test on olai's listener flips the row on after the bind and reads a login on the next upgrade.

15. **Machine-local state is one door, and it is called `LocalState`** ([juspay/olai#505](https://github.com/juspay/olai/pull/505), merged 2026-09-04: the rename, one file per plugin with chat's three sections, a save that settles with the write and fails with the leaf's reason, the migration table in core's door with an expiry and xyne's old record included, and the flip proved at the server's door. One PR, in parallel with #502 and phase 12.) *Ruled by the human, 2026-09-05, through the question tool: the door and its files are renamed from `Held`/`hold` to `LocalState`, and chat's three files merge into one file per plugin.*
   - **What exists.** `~/.local/state/olai/` (`$XDG_STATE_HOME/olai`) holds what this machine keeps for a served directory that is not configuration and that nothing could rebuild; each directory is a file named by a hash of its path (`c924c3e4709d8c9a` is `/home/srid/code/oss.olai`). Four kinds today: `chat/<hash>.json` (`{"cwd","agent","session"}`, which conversation the panel was in), `wake/<hash>.json` (`{"cwd","scopes":[{agent,session,plugin,file}…]}`, capped at 32, which file each conversation's doorbell points at), `heard/<hash>.json` (`{"cwd","heard":[{agent,session,taught,assigned?,said:{text,at}}…]}`, capped, the last line each conversation said), all three written by chat straight through `@olai/state`'s `readHeld`/`writeHeld` with a cap and a permit each (`memory.ts`, `scopes.ts`, `sessions.ts`); and `mirror/<hash>.json` written by xyne-spaces through the `Held` door (`@olai/plugin-api`'s one-record-per-plugin door, minted per plugin name so its write chain orders saves; `server/src/held.ts`). "Held" is the repository's own word for "a record this machine holds", chosen to contrast with configuration in the vault; the human found it cryptic.
   - **The rename.** `Held` becomes `LocalState` (the tag, the `PluginsConfig.heldFor` field, `server/src/held.ts`), `readHeld`/`writeHeld` become `readLocal`/`writeLocal`, `fileForHold` becomes `fileForLocal`, and a plugin's file lives at `~/.local/state/olai/<plugin>/<hash>.json`. The state package stays the leaf that spells the path; `pruneGone` walks the new layout. Old files under `chat/`, `wake/`, `heard/`, `mirror/` and `hold/` are read once and migrated on first write, or left inert with a line in the log; the lane says which and why.
   - **One file per plugin.** Chat's three records become three sections of one document at `~/.local/state/olai/chat/<hash>.json`: `{"cwd","memory":{agent,session},"wake":{scopes:[…]},"heard":{heard:[…]}}`. The caps and evictions stay per section; the three permits become the door's one write chain; a change to any section rewrites the whole file, which the human accepted. `memory.ts`, `scopes.ts` and `sessions.ts` keep their readings and their tests and lose their file I/O: each reads its section off `LocalState.load` and writes it through `LocalState.save` with the other two sections carried. Xyne-spaces' record is unchanged in shape and moves to the new path.
   - **Done means**: no plugin imports `@olai/state`; the fence holds it as an equality; the three chat records survive a chat flip (phase 8's switch) and a restart with the same contents; `docs/running.md` names the new directory layout and what an old layout does on first start.
   - **Not started**: core's own per-directory records (the one-brain lock, the git policy remnants) on the same door; a second consumer of the merged shape.
16. **Transports are rows, and profiles are bundles.** The websocket listener and the `/mcp` route become two rows, `ws` and `mcp`, each consuming the composed surface and exposing one face's map; `serve.ts` becomes a base bundle plus a profile's row list: `web` = base + `ws` + `mcp` + `web-app` + the tenants, `surface` = base + `mcp`. "The empty roster composes" stops being a defended special case. Done means: `olai surface` is a row list and not a branch in `serve.ts`; the headless scenarios pass unchanged; a serve with no transport row starts and says so. *[#524](https://github.com/juspay/olai/pull/524), review addressed 2026-09-05, good to merge: profiles are row lists, the transports are fibers over a shared listener, `mcp/endpoint.ts` holds the endpoint's two lifetimes, `--profile surface` on `olai web` is an accepted departure since `olai surface` became the terminal client, and the root's argument is back at 324 lines with the paragraphs this phase earned.* Touches `serve.ts` whole, so nothing runs beside it.
17. **The vault is a row.** The store is mounted from a row that provides `Vault` and `Kinds`, so a test-minimal profile is vault plus kinds and nothing else, and `bind()` stops capturing the store. Done means: `runtime.test.ts` mounts a profile instead of calling `bind()` with nulls. After 15.
18. **The shell's own faces are client plugins.** The outline view, preferences and the core dressings for `date`, `path`, `doc` and `ref` register into the same slots the tenants use (phase 9's route slot and the three `outline.row.*` slots), the way dsh's `ui-session` and `ui-conversation` are. Core's shell keeps the boxes: the layout, the router's parser, the pane, the palette's input. Done means: a core dressing and an agent-written one (phase 12) are the same kind of thing in the slot table. Last, and the largest; after 15.

**Dropped from the plan, 2026-09-04:** the orchestrator as a profile. The human is not interested now; the orchestrator's vault lives elsewhere and drives olai through the MCP face. If it returns, it is one more row list over phase 16's base bundle and a driver group of rows, which is what the profiles mechanism makes cheap, so nothing here has to anticipate it.

## 7. olai's rulings, re-tried

Each earlier ruling, and what a fresh look says. "Keep" means it survives on merit, not by precedent.

| Ruling (source) | Verdict | Why |
| --- | --- | --- |
| Compiled-in registry; "the boundary is the value, not the loading"; a third party rebuilds olai (`plugin-system.md` §What compiled-in still cannot do) | **Overturn** | The loader mounts a package by name from a config row. (`olai plugin add` was proposed here and dropped by ruling in phase 8: the panel is the switch, and out-of-tree packages wait for a second plugin to build from a pin.) Compiled-in was the cost of having no runtime, not a design. |
| The registry never grows a dependency arm; no plugin consumes a plugin; the root threads capabilities (eighth sitting) | **Overturn** | `inject` is the dependency arm, and it is reactive: the "half-wired state" the ruling feared is exactly `PENDING`, a legitimate, inspectable state with a theorem about when it resolves. `plugin-spaces-mirror` is the first edge that needs it. |
| Enable is CLI/nix only; preferences rows are read-only (#434's shape) | **Overturn** | The row is data on a config tree; a toggle writes `disabled` and the loader reconciles. The honest "who said so" survives as which layer carries the override, drawn on the plugins panel (`--dump-config` was dropped by ruling in phase 8). |
| Three rosters (`WIRES`, `SERVERS`, `PLUGINS`) must agree; six edits per plugin | **Overturn** | One config row. The three *doors* (wire, server, browser export conditions) stay, because that split is about module graphs, not registration. |
| `PluginServices` handed in whole; a plugin re-declares `AppFurniture` structurally | **Overturn** | Services on `ctx`, declared by merging, requested by `inject`. A plugin sees what it names. |
| `probe()` answers both halves at once | **Reframe** | The invariant survives as a property of one waterfall dispatch; the hand-written contract goes. |
| `wake` must be declared on the server door beside `probe` so `chat.scope` can refuse a plugin without one | **Reframe** | `ctx.wakes.register(...)`; `chat.scope` reads the registry. Same check, no field on a manifest. |
| The fence: no general package imports or spells a plugin (`fence.test.ts`) | **Keep** | dsh has the same physics ("no privileged core to patch"). Every claim survives except "no plugin imports a plugin", which becomes "a plugin imports the framework and the service definitions it names". |
| A plugin never imports `@olai/plugin-api` (cycle) | **Overturn** | The cycle existed because `plugin-api` was the registry. As service definitions it is what plugins import, the position `@kolu/surface` already holds. |
| `<plugin>-<kind>` prefixing; the vault's row beats the plugin's claim; the licence consult answers per drawn value | **Keep** | Vault judgement above Cordis's line; the paper "prescribes no concrete scenario" (§5). The composition happens in `ctx.kinds` from `ctx.fiber.name`, which is the registry binding, never the caller. |
| Failure prose is whole sentences owned by the plugin | **Keep** | Nothing in Cordis argues either way; the argument stands. |
| The doorbell cannot read; scope is manual per conversation | **Keep the first, overturn the second** | Write-only is a capability decision and stays. Manual scope was already superseded by node agents (scope is the subtree); under Cordis it is interception metadata on the session's context, derived, never a setting. *As built (phase 6): a per-session ticket checked in the write gate, not interception on a context; §10.* |
| Disabled means absent | **Keep, scoped** | True of registrations once teardown and reconciliation have settled: each is owned by the fiber's scope and its release removes it, and the rollback and teardown tests establish that the registries meet that contract. Not a claim about emissions already sent, and not proved by a fiber's state word alone (§4.1). |
| The browser receives answers, not rules (#395) | **Keep** | The server still answers the word; the browser Cordis app boots from the roster cell. |
| `@kolu/surface` is the house, not an appliance; never wrap it | **Reframe** | It stays the wire protocol. The house (what is mounted, when, with what) becomes Cordis. Whether siblings can change live is the spike's first question and a possible upstream ask. |
| No `plugin-orchestrator`; the driver is `olai orchestrate`, a wire client with handles minted at the root (eighth sitting) | **Overturn** | The argument was "population one, nothing to probe, the practice is vault data". The practice stays vault data. But a driver that listens to `vault/revision`, holds gate policy, and dispatches lanes is a *group of plugins* in an `orchestrate` profile, the way dsh's agent loop, compaction and plan mode are plugins. That is more testable than a separate process speaking the wire, and it is the only shape in which the orchestrator can later be a node agent (phase 5 of node agents). *Superseded: the orchestrator profile was dropped by the human on 2026-09-04 (the note after phase 18); this row is the record of the argument, not a decision.* |
| The receptacle stays home; `packages/plugins` never goes upstream | **Reframe** | Nothing of olai's goes upstream. Cordis comes *down*, as an npins pin hydrated like `@kolu/*`, never as a copied `vendor/` tree (§5). |
| One plugin per agent, never a roster plugin | **Keep** | Each is a fiber; `--plugins` per agent falls out; the paper's cadence argument (independent release clocks) is exactly why they are separate components. |
| kolu and odu land as one PR; property kinds solved now | **Keep as history** | Done and merged; this proposal builds on that tree. |

## 8. Questions the spike answered

Answers from [juspay/olai#472](https://github.com/juspay/olai/pull/472), each backed by a test in `packages/cordis-spike/src/*.test.ts` and re-verified independently (23 pass, fence 30 pass, typecheck clean, `cordis-deps` agrees).

- **Can `@kolu/surface` add and drop a sibling on a live group, and does `connectSurfaces` tolerate a roster change?** No, on both sides: the sibling map is baked at the call, server and client. Re-compose works as a full re-call; a roster change on the client is a reconnect. Framework ask filed first in phase 2 (§5).
- **Does the Proxy `ctx` cooperate with Effect, and does HMR work under Bun?** Proxy and Effect: yes at the access layer (`Effect.gen`, nested `runPromise`, disposers still run), which is what the overlay needs and not a shared lifecycle. HMR under Bun: no (§5).
- **Which registrations are revertible and which are emissions?** Revertible, proved by dispose restoring absence: `ctx.surfaces.register`, `ctx.kinds.register`, `ctx.on` listeners, and a loader row (`disabled: true` disposes the live fiber; a row is never `PENDING` with a half-installed surface). Emissions the runtime does not and must not revert: vault writes, doorbell deliveries, socket dials. No new compensation story is needed for phase 2; kinds validate as text when the plugin is off and a delivered sentence stays in the transcript.
- **Where is intercept metadata for the subtree fence consulted?** On a `vault` service wrapping the store, through `Service.resolveConfig` on the derived context, merged at use with no reload. Not in the store's write path: `store.ts`'s `commit` is Effect and does not read a Cordis context, and putting the fence there would be the store learning Cordis.
- **How does the browser Cordis app boot?** From the roster cell (`@olai/surface`'s `plugins`), now republished on every register/dispose. A served config filtered to the wire and browser doors would be a second roster in a second grammar. The three doors stay. Until the framework ask lands, the browser reconnects when the cell moves.

## 9. olai as a Cordis application

*Tagged optional on 2026-09-02 and approved by the human on 2026-09-04. Phases 1 to 8 make the plugin **system** a runtime; phases 9, 10 and 12 to 18 (§6, the promotion track) take the composition root apart one row at a time, and this section is the reasoning those phases stand on: which parts earn a fiber, which stay what they are, and what the profiles come to.*

**What phases 1 to 8 do not do.** After them, `runtime.ts`'s `bind()` and `serve.ts` are still one fixed function that composes the store, the documents and outlines surface, the chat, git, identity, index, ops, the MCP face and the websocket listener, and plugins are what it mounts. That root is not a component, so the paper's guarantees (recovery, ordering, confluence) stop at its edge: they hold for what is mounted and say nothing about the thing doing the mounting. dsh has no such edge; its session log, tool registry, model adapter and agent loop are rows, and `--dump-config` prints the product.

**What "everything is a plugin" means here, and what it does not.** Not atomization: dsh's core packages are large, and §6.5 of the paper prices finer grain in config and naming. It means three things.

1. **Every part of the root is a `Service` mounted from a row**, with `inject` and `provide`: `vault` (store and revisions), `kinds`, `surface-root`, `documents`, `outlines`, `chat` (sessions, deliveries, the session log), `agents` (engines), `git`, `identity`, `index`, `ops`, `listener`, `mcp`, `web-app`. `bind()`'s one closure, which today captures the store, the chat and every cell together, becomes the wiring those services declare. A part that unloads unwinds its registrations; a part that fails lands `FAILED` alone.
2. **Profiles as bundles.** `olai web`, `olai surface` (headless MCP) and a test-minimal profile are row lists stacking one base bundle. "The empty roster composes" stops being a defended special case: any subset composes, and a test mounts what it needs instead of `bind()` with nulls.
3. **Core's own surface members become contributions to the root.** The rooted bundle gives *siblings* a prefix; core's tags stay unprefixed and byte-identical. So this needs a root-level mount on `@kolu/surface` (unprefixed members merged into the root spec, collisions refused) beside the sibling `mount` kolu#2223 added. Until it exists the surface root stays one plugin, which is fine: replaceability from config is the property, not granularity.

**The enabling piece, an Effect bridge, exists.** `@olai/effect-cordis` (phase 4) is it: a plugin is an Effect with `needs`, its scope closes on dispose, and every Effect-managed resource is scope-owned, so that once a teardown has settled its release has undone it (§4.1 for the conditions; phase 12a for the teardown order and cancellation the bridge still owes). Chat (phase 7) was mounted rather than rewritten on it, which is the proof the track needed.

**What olai would not copy from dsh.** dsh's turn events (`agent/pre-step`, the `tools/*` waterfalls) exist because dsh owns the model loop. olai seats ACP engines and does not. The events olai would own are around session lifecycle: a durable session log with "model-visible means logged" asserted at runtime, a `chat/deliver` waterfall, a prompt-channel seam, and engines as providers on `ctx.agents`. A smaller event surface, and the right one.

**Which parts of the core to promote, and which to leave.** The paper gives four reasons a part earns a fiber, and a part with none of them is data or grammar and should stay what it is. (1) It *acquires* something with a lifetime: a socket, a subscription, a watcher, a child process, so revertible effects replace a hand-written teardown. (2) It is a *seam* with more than one plausible provider, so a row can swap it and the paper's exclusive-binding pattern applies. (3) It is *policy* that composes: several listeners on one event, each replaceable alone. (4) It is instantiated *per scope*: per node agent, per session, per tab. Measured against olai's core (line counts are non-test source, comments included):

| Part of the core (today) | Lines | Reason | Would become | What it enables |
| --- | --- | --- | --- | --- |
| `chat` (all of it) | 13,900 + 16,700 in the tab | 1, 2, 3, 4 | **Phases 6 and 7**, landed 2026-09-04: sessions as fibers under per-node scopes first, in place; then a plugin with both halves, a sibling on the wire, providing the doors it owns | The first part of the core to become a row; `olai surface` and `--plugins=` stop being special cases |
| Engine legs (`chat/src/agents/{claude,opencode,pi}`) | ~1,500 | 2 | one plugin per engine on `ctx.agents` | Already phase 5 |
| `git` (the ledger) | 1,073 | 1, 2 | **Phase 10**: a `Ledger` door in core with `git` as its provider row | `GIT_OFF` becomes "no provider mounted"; a second backend is a row, not a mode |
| The listener (websocket + `/mcp`) and the MCP face (`server/src/listener.ts`, `mcp/`) | ~2,000 | 2, 3 | **Phase 16**: transport rows (`ws`, `mcp`) and profiles as row lists | `olai web` and `olai surface` become profiles that differ by which transport rows they carry, instead of one root with nulls |
| `index` (search) | 891 | 2 | **Phase 13**: a `Search` door with the core matcher as its provider row | The parked semantic recall (#165) returns as a second provider row, with no core PR and no ruling to reopen |
| `identity` | 429 | 2 | **Phase 14**: an `Identity` door; tailscale as one provider row | The sitting's own "probe-shaped someday", without waiting for the someday |
| `state` (machine-local state) and `held` | ~500 | 1, 4 | **Phase 15** (merged, #505): one `LocalState` door every row and every core part keeps its record on | Every plugin's machine-local record on one door |
| The tab's shell (sidebar, outline view, chat panel, preferences, the agents section, the day page — the last three are rows after phases 7 and 9) | most of `web` | 3, 4 | **Phases 9 and 18**: the journal first (it forces the route slot), then the shell's own faces, the way dsh's `ui-session` and `ui-conversation` are | Core's own dressings (date, path, doc, ref) register like a plugin's, so an agent-written dressing (phase 12) is the same kind of thing as a shipped one |
| The orchestrator drivers (wake listener, gate policy, dispatch tools, still unbuilt) | 0 | 1, 3 | dropped from the plan 2026-09-04; if it returns, a driver group over phase 16's base bundle | Distant future, on the human's word |
| `vault` (the store's revision publisher) | part of `store` | 1 | **Phase 17**: `Vault` exists; the store becomes a single provider mounted from a row | The test-minimal profile: vault plus kinds and nothing else |

**And what to leave alone.** `format` (33,580 lines) is the vault's grammar: kinds, validation, the meaning consult. It has no lifetime, no second provider, no policy and no scope; it is the language every plugin speaks, and a language is not a component. `ops` is the same one floor up: the operations on a vault are the vocabulary of writes. `surface` is the wire contract. `acp` is the protocol. `fonts`, `child`, `sigterm`, `log` are utilities. Making any of these a fiber would be granularity for its own sake, the cost §6.5 of the paper prices and dsh avoids: dsh's `session`, `tools` and `agent-loop` are large packages, each one row.

**The profiles this yields (phase 16).** `web` = vault, kinds, chat, agents, git, identity, search, `ws`, `mcp`, web-app, journal, plus the tenants. `surface` = vault, kinds, `mcp`. `test-minimal` = vault, kinds. Each is a row list, the plugins panel draws it (`--dump-config` was dropped by ruling), and "the empty roster composes" stops being a defended special case because every profile is a subset.

**Cost.** `runtime.ts` is the largest single refactor olai has attempted, and phase 7 took the biggest bite of it (six hundred lines of one feature out of the two files every feature passes through). The §6.5 integration-component tax lands wherever chat, store and git interleave in one closure today, and each such knot becomes a small integration plugin. The Effect bridge is built; the one framework ask (the root-level mount) is phase 10's ruling, and it gates every core member on the agent face.

**Why it was taken.** The third trigger fired: node agents forced chat into services (phases 6 and 7), the first step of this track was already taken, and the human chose to keep going (2026-09-04). What is sequenced in §6 is the rest, smallest first, each one a row the panel can switch off.

## 10. What of Cordis olai uses, defers, and leaves out

*Added 2026-09-04 on the human's approval, after [juspay/oss.olai#1](https://github.com/juspay/oss.olai/issues/1) (finding 6): the accepted architecture in one table, so the proposal text above (§§2–4, written before anything was built) is read as history and this is read as current.*

| Cordis capability | olai today | Status |
| --- | --- | --- |
| Fibers with LOADING / ACTIVE / PENDING / FAILED and reactive activation on `inject` | Every row and every dynamic plugin is a fiber; `needs` is `inject`; the panel draws the states | **Used** |
| Revertible effects (every registration undone on dispose) | Every registration is an `acquireRelease` on the fiber's Effect scope; the bridge owns the scope | **Used, adapted** (Effect scopes carry what `ctx.effect` would) |
| Declarative loader with rows, `disabled`, `config`, patches | `olai.yml` rows, `--plugins` and `--commit`/`--push` as patches, `Config` schemas | **Used** |
| Services provided by a plugin for others | Five core-owned keys through `Offers`, provided on the offering fiber since phase 12a | **Used, limited**; plugin-owned keys are phase 12b |
| Confluence and failure containment (the paper's theorems) | Claimed for registrations under the paper's conditions; evidenced by the flip and restart tests | **Claimed, scoped** (§2, §4.1, §7) |
| Cancellation of a loading fiber; host shutdown | The bridge keeps the initialisation as an interruptible Effect fiber and reaches it from stop, flip, host close and dependency withdrawal; `closeHost` drains every fiber (phase 12a, merged) | **Used, adapted** (bridge-owned, with one named coupling to the pin) |
| Interception (`ctx.intercept`, consulted at use, changed without reload) | A per-session ticket checked in the write gate: policy at invocation, per session, not on a context | **Adapted**; context interception omitted |
| Isolation realms (`ctx.isolate`) | None; two engines are two rows, not two realms | **Omitted** |
| Child contexts and `ctx.extend` | Effect scopes per node agent and per session | **Adapted** |
| Events (`ctx.on` / `emit`) | Replaced by doors and one waterfall (§4.3); no bare Cordis events cross a plugin boundary | **Deliberately not used** |
| HMR / `fiber.update` on code change | None; a version of a dynamic plugin is a new URL and a dispose-then-mount | **Omitted** (no Bun cache bust exists) |
| Cycle diagnosis at load | Not verified against the pinned runtime; not claimed until a test shows it | **Unverified** |
